# v2.0 Requirements — ARCHIVED (shipped 2026-05-30)

> Snapshot of the v2.0 requirement set with final status. Live requirements (incl. the v2.1 carry-over backlog) remain in `.planning/REQUIREMENTS.md` until `/gsd-new-milestone` resets them.

**Milestone goal:** better-sourced corpus + accuracy gain, explainability + top-N, v1 bug sweep, visual polish, full front-end redesign.

## Final status by requirement

### Bug-Fix Sweep (Phase 6) — all complete
- [x] **BUG-01** — H₂ UI/toggle/backend removed; H₀ also removed; UI ships H₁-only (H₂ → v3).
- [x] **BUG-02** — persistence-diagram dots scale by √persistence; ∞ points on a dedicated marker.
- [x] **BUG-03** — BookSlider on `GET /api/corpus/genres/{genre}/books` (title + author + word count).
- [x] **BUG-04** — ROADMAP.md/STATE.md restored; pre-commit hook rejects 0-byte `.planning/**/*.md`.
- [x] **BUG-05** — `cache_key` includes `corpus_hash` + `w2v_model_sha256` (landed before Phase 8 retrain).

### Corpus Sourcing Research Spike (Phase 7) — all complete
- [x] **RES-01** — `CORPUS_SOURCING.md` (sources, per-genre counts, author-distribution).
- [x] **RES-02** — `VALIDATION_PROTOCOL.md` (v1-frozen test set, GroupKFold-by-author, macro-F1, permutation test).
- [x] **RES-03** — multi-label decision: deferred to v3, documented.

### Corpus Expansion (Phase 8 / 8.1)
- [x] **CEXP-01** — `corpus/books.yaml` restructured (8 v2 genres, author + word_count + source); 154 verified-clean books post-8.1.
- [x] **CEXP-02** — retrained Word2Vec + SVM end-to-end; assets shipped via `v2.0-data` Release (corpus_hash 3f4fe940…, w2v_sha cd81f9e6…).
- [x] **CEXP-03** — v2 macro-F1 0.7367 > v1 0.3235, permutation p=0.0010 (validated with anti-leakage disclaimer).
- [ ] **CEXP-04** — **BLOCKED.** GroupKFold-by-author 0.2865 vs hold-out 0.7367 = 45pp gap (>> 15pp threshold). Author leakage; shipped with D-31 disclaimer. → v2.1 (V21-01).
- [x] **CEXP-05** *(P2)* — deterministic `scripts/build_corpus.py`.

### Classification Depth (Phase 9) — all complete
- [x] **DEPTH-01** — top-N calibrated (`SVC(probability=True)` libsvm Platt, Brier 0.0481, sum-to-1).
- [x] **DEPTH-02** — top-N honest probability bars (no pie charts, no hidden predictions).
- [x] **DEPTH-03** — "Why this genre?" → `POST /api/classify/{job_id}/explain` (~15 ms p50, Redis-cached).
- [x] **DEPTH-04** — 3–5 nearest training books with Euclidean distance (L2-normalized features).
- [x] **DEPTH-05** — per-track contribution (topology vs vocabulary), zero-ablation.
- [x] **DEPTH-06** *(P2)* — TF-IDF driving words with "proxy, not literal inputs" disclosure.
- [x] **DEPTH-07** *(P2)* — entropy/uncertainty badge for ambiguous predictions.

### Visual Polish (Phase 10) — all complete
- [x] **POLISH-01** — light/dark/system via `preferencesStore` (Zustand persist).
- [x] **POLISH-02** — scene bg + all sidebar/topology/compare honor theme; imperative `scene.background` (no remount).
- [x] **POLISH-03** — first-load 3–5 step tour on `data-tour-id` anchors; skippable + replayable.
- [x] **POLISH-04** — tour covers scatter/genre/upload/topology (UI, not math).
- [x] **POLISH-05** — empty states for upload zone / compare / classification failure / explain.

### Onboarding & Theme Defaults (Phase 11) — all complete
- [x] **ONBOARD-01** — light default for new users (no dark flash).
- [x] **ONBOARD-02** — first-visit / ≥30-day How-It-Works → tour chain (`introSeenAt`).
- [x] **ONBOARD-03** — early-dismiss still chains; manual open does not.

### The Reading Room — full front-end redesign (Phase 12) — all complete
- [x] **RR-01** — editorial design system + shell (masthead, footer, footnotes, Tweaks).
- [x] **RR-02** — Collection (reskinned R3F plate + catalog rail + marginalia + filter + Find).
- [x] **RR-03** — Catalog card (letterpress card + leader-line plate detail).
- [x] **RR-04** — Comparative Study folio.
- [x] **RR-05** — Submit a Text → The Reading (real `useClassify`/`useExplain`, "marginal" voice).
- [x] **RR-06** — Topology reading-room skin (accent ε, genre-hex heatmap, H₁ only).
- [x] **RR-07** — About + Guide (3 tabs + 5 live figures, auto-open once).
- [x] **RR-08** — 6-stop guided tour over the real screens (four-panel spotlight).
- [x] **RR-09** — responsive + animation-robustness pass.

## Coverage
25 v2 requirements (BUG/RES/CEXP/DEPTH/POLISH) defined at milestone start + ONBOARD-01..03 (Phase 11) + RR-01..09 (Phase 12) added mid-milestone. **All complete except CEXP-04 (BLOCKED, shipped-with-disclaimer, → v2.1).**

---
*Archived 2026-05-30 at v2.0 milestone close.*
