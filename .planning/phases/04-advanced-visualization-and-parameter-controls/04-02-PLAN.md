---
phase: 04-advanced-visualization-and-parameter-controls
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/pipeline/precompute_vr.py
  - backend/pipeline/tests/test_precompute_vr.py
  - backend/api/routes/viz.py
  - frontend/src/components/topology/VRViewer.tsx
  - frontend/src/components/topology/VREdges.tsx
  - frontend/src/components/topology/EpsilonSlider.tsx
  - frontend/src/components/topology/TopologyPanel.tsx
  - frontend/src/hooks/useVRData.ts
  - frontend/src/lib/vrFiltering.ts
  - frontend/src/lib/vrFiltering.test.ts
autonomous: true
requirements:
  - TOPO-03
  - TOPO-04
  - TOPO-05
must_haves:
  truths:
    - "User sees a 3D VR viewer in the right panel of the Topology tab showing word points with edges"
    - "User drags epsilon slider and edges appear/disappear in real time with no server calls"
    - "Edges born at the current epsilon value flash yellow (#FACC15) for 500ms before fading to default"
    - "H0 edges render in subdued color (#4A4A5A); H1/H2 boundary edges near current epsilon render in highlight (#FACC15)"
    - "VR viewer has its own independent orbit camera separate from scatter"
  artifacts:
    - path: "backend/pipeline/precompute_vr.py"
      provides: "VR edge precomputation from distance matrix with feature_type labeling"
    - path: "frontend/src/components/topology/VRViewer.tsx"
      provides: "R3F Canvas with word points and edge rendering"
    - path: "frontend/src/components/topology/VREdges.tsx"
      provides: "THREE.LineSegments component for filtration edges with feature_type coloring"
    - path: "frontend/src/lib/vrFiltering.ts"
      provides: "Browser-side edge filtering by epsilon threshold with feature_type-aware coloring"
    - path: "backend/api/routes/viz.py"
      provides: "GET /viz/vr/{genre} endpoint for VR edge data"
  key_links:
    - from: "frontend/src/components/topology/VREdges.tsx"
      to: "frontend/src/lib/vrFiltering.ts"
      via: "filterEdgesByEpsilon called in useFrame loop"
      pattern: "filterEdgesByEpsilon"
    - from: "frontend/src/hooks/useVRData.ts"
      to: "/viz/vr/{genre}"
      via: "React Query fetch"
      pattern: "useQuery.*vr-edges"
    - from: "backend/pipeline/precompute_vr.py"
      to: "backend/pipeline/homology.py"
      via: "build_weighted_distance_matrix import"
      pattern: "build_weighted_distance_matrix"
---

<objective>
Build the animated Vietoris-Rips filtration viewer: backend precomputation of VR edges with feature_type labeling (H0/H1/H2 boundary membership), API endpoint, and frontend 3D viewer with epsilon slider and birth/death highlighting. The VR viewer occupies the right panel of the Topology tab (left panel = persistence heatmap from Plan 01).

Purpose: The VR viewer lets users directly observe how topological features form as the filtration radius increases — this is the most interactive and visually impactful component of the topology tab.
Output: Working VR 3D viewer with epsilon slider, precomputed edge data (including feature_type per CONTEXT.md) served from backend, and real-time browser-side edge filtering with feature_type-aware coloring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-visualization-and-parameter-controls/04-CONTEXT.md
@.planning/phases/04-advanced-visualization-and-parameter-controls/04-RESEARCH.md
@.planning/phases/04-advanced-visualization-and-parameter-controls/04-UI-SPEC.md
@.planning/phases/04-advanced-visualization-and-parameter-controls/04-01-SUMMARY.md

@frontend/src/components/canvas/PointCloud.tsx
@frontend/src/components/canvas/ScatterCanvas.tsx
@frontend/src/stores/visualizationStore.ts
@backend/pipeline/homology.py
@backend/pipeline/precompute_viz.py
@backend/api/routes/viz.py
@backend/api/app.py
@backend/cache/store.py

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From frontend/src/stores/visualizationStore.ts (extended by Plan 01):
```typescript
// New Phase 4 slices added by Plan 01:
activeTab: 'scatter' | 'topology' | 'compare'
selectedHomologyDim: 0 | 1 | 2
vrEpsilon: number        // read by VREdges for filtering
compareMode: boolean
compareGenre: string | null
settingsDrawerOpen: boolean
h2Enabled: boolean
// ... plus setters
```

From backend/pipeline/homology.py:
```python
def build_weighted_distance_matrix(vectors: np.ndarray, tfidf_weights: np.ndarray) -> np.ndarray:
    """Weighted distance: D_ij = ||v_i - v_j|| / (w_i + w_j), w normalized to [0,1]."""
```

From backend/cache/store.py:
```python
def cache_key(prefix: str, params: dict) -> str: ...
def cache_put(key: str, data) -> None: ...
def cache_get(key: str) -> Any: ...
def cache_exists(key: str) -> bool: ...
```

VR Edge payload format (per CONTEXT.md locked decision — payload includes feature_type):
```typescript
interface VRPayload {
  words: string[]           // word list for index mapping
  edges: [number, number, number, number][]  // [word_a_idx, word_b_idx, eps_birth, feature_type] compact tuples
  // feature_type: 0=H0 edge (generic/connected component), 1=H1 boundary edge, 2=H2 boundary edge
  epsilon_max: number
  positions: [number, number, number][]  // 3D coords from current projection
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend VR edge precomputation with feature_type labeling and API endpoint</name>
  <files>
    backend/pipeline/precompute_vr.py,
    backend/pipeline/tests/test_precompute_vr.py,
    backend/api/routes/viz.py
  </files>
  <behavior>
    - Test: precompute_vr_edges with 10 random vectors returns edges sorted by eps_birth ascending
    - Test: precompute_vr_edges only includes edges with distance <= epsilon_max
    - Test: precompute_vr_edges with epsilon_max=0 returns empty edge list
    - Test: each edge tuple has 4 elements: [int, int, float, int] (a_idx, b_idx, eps_birth, feature_type)
    - Test: feature_type values are in {0, 1, 2} (H0, H1, H2)
    - Test: GET /viz/vr/{genre} returns JSON with words, edges, epsilon_max, positions keys
    - Test: GET /viz/vr/invalid_genre returns 404
    - Test: edges array contains tuples of [int, int, float, int] with indices within word list range
  </behavior>
  <action>
1. **Create `backend/pipeline/precompute_vr.py`**:
   - Follow the existing `precompute_viz.py` pattern (imports, logging, cache usage).
   - Import `build_weighted_distance_matrix` from `backend.pipeline.homology`.
   - Main function `precompute_vr_edges(genre, words, vectors, tfidf_weights, epsilon_max, projection_coords, homology_dims=[0, 1])`:
     - Build weighted distance matrix using existing `build_weighted_distance_matrix`.
     - Run ripser on the distance matrix to get persistence diagrams for the requested dimensions.
     - From the ripser output, identify which edges participate in H1/H2 boundaries:
       - For each persistence pair (birth, death) in dimension d, the birth edge is the edge at distance = birth value. Use the distance matrix to find the edge (i, j) whose distance matches the birth value (within floating-point tolerance 1e-8).
       - Label these edges with `feature_type = d` (1 for H1, 2 for H2).
     - Extract upper triangle: for each pair (i, j) where i < j, if `dist_matrix[i,j] <= epsilon_max`, emit edge `[i, j, round(dist_matrix[i,j], 5), feature_type]`.
       - `feature_type = 0` for edges not identified as H1/H2 boundary births (generic H0/connected component edges).
       - `feature_type = 1` for edges that are H1 cycle birth edges.
       - `feature_type = 2` for edges that are H2 void birth edges.
     - Sort edges by eps_birth ascending (critical for drawRange optimization in browser).
     - Return `{words: list[str], edges: list[list], epsilon_max: float, positions: list[list]}`.
   - Limit to top 500 words by TF-IDF weight per genre (per CORPUS-03 max_words cap) to keep payload tractable.
   - Entry point `precompute_all_vr(window, projection, force)`:
     - Load Word2Vec model, books.yaml, TF-IDF data (same pattern as precompute_viz.py).
     - For each genre: select top-500 TF-IDF words, get their vectors and projection coords.
     - Call `precompute_vr_edges`, cache result as `vr_edges:{genre}:{projection}:{window}`.
     - Also support per-book: cache as `vr_edges_book:{gutenberg_id}:{projection}:{window}`.
   - Add `get_cached_vr_edges(genre, projection, window)` helper.
   - Call from `precompute_viz.py` main flow or as standalone: `python -m backend.pipeline.precompute_vr`.

2. **Create `backend/pipeline/tests/test_precompute_vr.py`**:
   - Test with synthetic data: 10 random 50D vectors, known TF-IDF weights.
   - Assert edges sorted by eps_birth. Assert no edge exceeds epsilon_max.
   - Assert edge indices are valid (0 <= idx < n_words).
   - Assert each edge is a 4-element tuple: [int, int, float, int].
   - Assert feature_type values are in {0, 1, 2}.
   - Assert epsilon_max=0 returns empty edges.
   - Test with a simple geometric configuration (e.g., points forming a known H1 loop) to verify at least one edge gets feature_type=1.

3. **Add VR endpoint to `backend/api/routes/viz.py`**:
   - `GET /viz/vr/{genre}?projection=pca` -- returns cached VR edge data.
   - Validate genre against `_KNOWN_GENRES`.
   - Validate projection in {'pca', 'kpca', 'umap', 'tsne'}.
   - Response is the cached dict: `{words, edges, epsilon_max, positions}`.
   - Apply gzip compression via `Response(content=gzipped_json, media_type='application/json', headers={'Content-Encoding': 'gzip'})` for payloads > 100KB.
   - 503 if not cached (same pattern as scatter endpoint).
  </action>
  <verify>
    <automated>cd backend && python -m pytest pipeline/tests/test_precompute_vr.py tests/ -x -q --tb=short</automated>
  </verify>
  <done>
    - precompute_vr_edges produces sorted edge list filtered by epsilon_max
    - Each edge is [idx_a, idx_b, eps_birth, feature_type] (4-element tuple per CONTEXT.md)
    - feature_type correctly labels H0 (0), H1 (1), H2 (2) boundary edges
    - GET /viz/vr/romance?projection=pca returns valid VR payload
    - GET /viz/vr/invalid_genre returns 404
    - All backend tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend VR viewer with epsilon slider, feature_type coloring, and birth/death highlighting</name>
  <files>
    frontend/src/lib/vrFiltering.ts,
    frontend/src/lib/vrFiltering.test.ts,
    frontend/src/hooks/useVRData.ts,
    frontend/src/components/topology/VREdges.tsx,
    frontend/src/components/topology/VRViewer.tsx,
    frontend/src/components/topology/EpsilonSlider.tsx,
    frontend/src/components/topology/TopologyPanel.tsx
  </files>
  <behavior>
    - Test: filterEdgesByEpsilon with eps=0 returns count=0
    - Test: filterEdgesByEpsilon with eps=1.0 on edges [0.2, 0.5, 0.8, 1.5] returns count=3
    - Test: filterEdgesByEpsilon marks edges within birthWindow of current eps as highlighted (sets color to #FACC15 RGB values)
    - Test: edges with feature_type=0 (H0) get subdued color #4A4A5A when not at birth threshold
    - Test: edges with feature_type=1 or 2 (H1/H2 boundary) get highlight color #FACC15 when born near current epsilon
    - Test: edges array is pre-sorted by eps_birth, so filtering can use binary search / early exit
    - Test: getVisibleEdgeCount returns correct count for given epsilon
  </behavior>
  <action>
1. **Create `vrFiltering.ts`**:
   - Export `filterEdgesByEpsilon(edges, epsilon, positions, birthWindow = 0.005)`:
     - Edges are `[word_a_idx, word_b_idx, eps_birth, feature_type][]`, PRE-SORTED by eps_birth.
     - The 4th element `feature_type` is: 0=H0 edge (generic), 1=H1 boundary edge, 2=H2 boundary edge.
     - Since edges are sorted, use binary search to find the cutoff index where `eps_birth > epsilon`. Only iterate up to that index (early exit optimization).
     - Pre-allocate `Float32Array(maxEdges * 6)` for positions and colors.
     - For each visible edge: copy start/end vertex positions from the positions array.
     - For edge coloring, apply feature_type-aware logic:
       - **H0 edges (feature_type=0):** Default color `#4A4A5A` (r=0.29, g=0.29, b=0.35). If `|eps_birth - epsilon| < birthWindow`, use birth highlight `#FACC15` (r=0.98, g=0.80, b=0.08).
       - **H1/H2 boundary edges (feature_type=1 or 2):** Default color `#4A4A5A` (same subdued). If `|eps_birth - epsilon| < birthWindow`, use bright highlight `#FACC15` (r=0.98, g=0.80, b=0.08) -- these are the topologically significant birth events.
     - Return `{linePositions: Float32Array, lineColors: Float32Array, count: number}`.
   - Export `getVisibleEdgeCount(edges, epsilon)`: binary search for count (used by UI to show edge count label).
   - CRITICAL: No server calls. This runs in the render loop at 60fps.

2. **Create `vrFiltering.test.ts`**: Unit tests for filterEdgesByEpsilon and getVisibleEdgeCount.
   - Test with 4 edges at eps_birth [0.2, 0.5, 0.8, 1.5] with feature_types [0, 1, 0, 2], epsilon=1.0 -> 3 visible.
   - Test birth highlight: H0 edge (feature_type=0) at eps_birth=1.0, epsilon=1.0 -> highlighted color #FACC15.
   - Test birth highlight: H1 edge (feature_type=1) at eps_birth=0.5, epsilon=0.5 -> highlighted color #FACC15.
   - Test non-birth H0 edge: feature_type=0, eps_birth=0.2, epsilon=1.0 -> subdued color #4A4A5A.
   - Test epsilon=0 -> 0 visible.
   - Test all edges visible when epsilon >= max eps_birth.

3. **Create `useVRData.ts`**: React Query hook.
   ```typescript
   export function useVRData(genre: string | null, projection: ProjectionKey)
   ```
   - Query key: `['vr-edges', genre, projection]`.
   - Fetches from `/viz/vr/${genre}?projection=${projection}`.
   - `staleTime: Infinity` (precomputed).
   - `enabled: !!genre`.
   - Returns typed `VRPayload` data (edges are `[number, number, number, number][]`).

4. **Create `VREdges.tsx`**: R3F component using `THREE.LineSegments`.
   - Props: `edges`, `positions` (from VR payload), `epsilon` (from store).
   - `edges` type: `[number, number, number, number][]` (4-tuple with feature_type).
   - Creates `THREE.BufferGeometry` with pre-allocated position and color attributes sized for max edge count.
   - In `useFrame`: read `vrEpsilon` from `useVisualizationStore.getState()` (hot-path, no subscription).
   - Call `filterEdgesByEpsilon` to get visible line positions and colors (feature_type-aware coloring applied by vrFiltering.ts).
   - Update geometry attributes: set position array, color array, and `geometry.setDrawRange(0, count * 2)` (2 vertices per line segment).
   - Mark `.needsUpdate = true` on both attributes.
   - Material: `THREE.LineBasicMaterial({ vertexColors: true, transparent: true, opacity: 0.7 })`.
   - Birth/death fade: track `highlightTimestamps` map. When an edge transitions from not-visible to visible (birth), record timestamp. In useFrame, edges within 500ms of birth get highlight color; after 500ms, fade to default over 300ms. Use lerp on the color buffer.

5. **Create `VRViewer.tsx`**: R3F Canvas wrapper for VR panel.
   - Separate `<Canvas>` instance from scatter (per CONTEXT.md).
   - Props: none (reads from store).
   - Reads `selectedGenre`, `projection` from store.
   - Uses `useVRData(selectedGenre, projection)`.
   - Renders word points using simplified point rendering (all points at 40% opacity, selected genre at 80%). Use `THREE.Points` with basic ShaderMaterial (simpler than scatter PointCloud -- no TF-IDF brightness needed here).
   - Renders `<VREdges>` with loaded edge data.
   - Own `<OrbitControls>` (independent camera from scatter).
   - Canvas props: `gl={{ antialias: true, alpha: false, preserveDrawingBuffer: true }}` (preserveDrawingBuffer for PNG export in Plan 03).
   - Camera: PerspectiveCamera fov=60.
   - Background color: `#0A0A0F`.
   - Empty state: show points with no edges + text overlay "Drag the epsilon slider to explore filtration" when epsilon=0.

6. **Create `EpsilonSlider.tsx`** (per TOPO-04):
   - shadcn Slider or custom range input.
   - Label: "Filtration radius (epsilon)" + current value in mono font.
   - Range: 0 to `epsilon_max` from VR data. Step: `epsilon_max / 200`.
   - Track: `#2A2A3A` bg, `#FACC15` fill.
   - Thumb: 16px circle, `#F5F5FF` fill, `#FACC15` border.
   - On change: `setVrEpsilon(value)` in store. NO server calls. NO debounce (render loop reads directly).
   - Show edge count: "{N} edges visible" below slider in muted text.
   - `aria-label="Filtration radius epsilon"`.

7. **Wire into `TopologyPanel.tsx`**: Replace right panel VR placeholder with `<VRViewer />` + `<EpsilonSlider />` below it.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/vrFiltering.test.ts --reporter=verbose && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - VR viewer renders word points and edges in Topology tab right panel
    - Dragging epsilon slider makes edges appear/disappear in real time
    - H0 edges (feature_type=0) render in subdued #4A4A5A; H1/H2 boundary edges highlight in #FACC15 when born near current epsilon
    - Birth edges flash yellow (#FACC15) for 500ms then fade to default
    - VR viewer has independent orbit controls from scatter
    - vrFiltering.test.ts passes: correct edge counts and feature_type-aware coloring for various epsilon values
    - No server calls occur during epsilon slider drag
    - Edge payload uses 4-element tuples [idx_a, idx_b, eps_birth, feature_type] per CONTEXT.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /viz/vr/{genre} | Genre name + projection query param from user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Tampering | /viz/vr/{genre} | mitigate | Validate genre against _KNOWN_GENRES; validate projection against literal enum {'pca','kpca','umap','tsne'} |
| T-4-05 | Denial of Service | VR precompute | accept | Runs at build time only, not user-triggered; max 500 words per genre caps computation |
| T-4-06 | Denial of Service | VR edge payload size | mitigate | Limit to 500 words (124,750 max edges), filter by epsilon_max (typically 50-90% reduction), gzip response |
</threat_model>

<verification>
1. `cd frontend && npx vitest run --reporter=verbose` -- all frontend tests green
2. `cd backend && python -m pytest -x -q` -- all backend tests green
3. Navigate to Topology tab: left panel shows heatmap (Plan 01), right panel shows VR viewer
4. Select a genre -> VR viewer loads edge data and shows word points
5. Drag epsilon slider -> edges appear progressively; H0 edges in subdued color, H1/H2 boundary births in yellow highlight
6. VR viewer orbit camera works independently from scatter
</verification>

<success_criteria>
- VR 3D viewer renders word points + edges in Topology tab right panel
- Epsilon slider controls edge visibility in real time (browser-side filtering, no API calls)
- Edge payload includes feature_type (4th element) per CONTEXT.md locked decision
- H0 edges colored #4A4A5A (subdued); H1/H2 boundary edges at birth colored #FACC15 (highlight)
- Birth/death events highlighted with 500ms hold + fade
- VR data precomputed per genre, served via GET /viz/vr/{genre}
- Independent orbit camera on VR viewer
- All tests pass (vrFiltering with feature_type tests, backend precompute_vr)
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-visualization-and-parameter-controls/04-02-SUMMARY.md`
</output>
