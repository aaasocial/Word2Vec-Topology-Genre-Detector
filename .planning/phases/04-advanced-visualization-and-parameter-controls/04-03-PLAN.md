---
phase: 04-advanced-visualization-and-parameter-controls
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - frontend/src/components/compare/CompareControls.tsx
  - frontend/src/components/compare/CompareHeatmaps.tsx
  - frontend/src/components/settings/SettingsDrawer.tsx
  - frontend/src/components/settings/SlowTierParams.tsx
  - frontend/src/components/settings/VerySlowTierParams.tsx
  - frontend/src/components/settings/RecomputeOverlay.tsx
  - frontend/src/components/explanation/PipelineExplanation.tsx
  - frontend/src/components/explanation/steps/Step1WordEmbed.tsx
  - frontend/src/components/explanation/steps/Step2TfidfWeight.tsx
  - frontend/src/components/explanation/steps/Step3PointCloud.tsx
  - frontend/src/components/explanation/steps/Step4Homology.tsx
  - frontend/src/components/explanation/steps/Step5PersistenceImage.tsx
  - frontend/src/components/explanation/steps/Step6Classification.tsx
  - frontend/src/components/sidebar/Sidebar.tsx
  - frontend/src/lib/exportUtils.ts
  - frontend/src/lib/exportUtils.test.ts
  - frontend/src/hooks/useRecompute.ts
  - frontend/src/App.tsx
  - frontend/src/components/canvas/ScatterCanvas.tsx
  - frontend/src/components/canvas/PointCloud.tsx
  - backend/api/routes/viz.py
autonomous: false
requirements:
  - COMP-01
  - COMP-02
  - PARAM-03
  - PARAM-04
  - PARAM-05
  - PARAM-06
  - EXPLAIN-01
  - UX-03
  - UX-05
must_haves:
  truths:
    - "User can select two genres and see dual brightness in scatter + stacked heatmaps in sidebar with consistent color scale"
    - "User can open settings drawer and adjust slow-tier params with explicit Recompute button"
    - "Very-slow-tier params show warning and confirm dialog before triggering retrain"
    - "Current visualization remains interactive during recomputation (dim overlay + Updating badge)"
    - "User can open a 6-step pipeline explanation dialog navigable by keyboard"
    - "User can export scatter as PNG and persistence data as CSV"
    - "Disclaimer banner is visible on Scatter and Topology tabs"
  artifacts:
    - path: "frontend/src/components/compare/CompareControls.tsx"
      provides: "Compare toggle + second genre picker"
    - path: "frontend/src/components/settings/SettingsDrawer.tsx"
      provides: "shadcn Sheet with slow + very-slow tier parameter controls"
    - path: "frontend/src/components/explanation/PipelineExplanation.tsx"
      provides: "6-step fullscreen slide deck dialog"
    - path: "frontend/src/lib/exportUtils.ts"
      provides: "PNG and CSV export helper functions"
    - path: "frontend/src/hooks/useRecompute.ts"
      provides: "Recompute trigger hook using existing arq/Redis/WebSocket pattern"
  key_links:
    - from: "frontend/src/components/compare/CompareHeatmaps.tsx"
      to: "frontend/src/lib/heatmap.ts"
      via: "renderHeatmap with shared vmin/vmax"
      pattern: "computeMinMax.*renderHeatmap"
    - from: "frontend/src/hooks/useRecompute.ts"
      to: "/viz/recompute"
      via: "POST request triggering arq job"
      pattern: "POST.*recompute"
    - from: "frontend/src/components/settings/SettingsDrawer.tsx"
      to: "frontend/src/stores/visualizationStore.ts"
      via: "dirtyParams tracking and isRecomputing state"
      pattern: "dirtyParams.*isRecomputing"
---

<objective>
Build genre comparison mode (dual brightness + stacked heatmaps), settings drawer with tiered parameter controls and recomputation, pipeline explanation dialog, export functionality, and the recomputation overlay. This is the largest plan in Phase 4 and completes all remaining requirements.

Purpose: This plan delivers the remaining Phase 4 features that depend on both the persistence heatmap (Plan 01) and VR viewer (Plan 02) being in place. Compare mode uses heatmaps; settings drawer triggers recomputation that affects all panels; pipeline explanation references all visualization types.
Output: Complete Phase 4 feature set: compare mode, settings drawer, pipeline explanation, export, and recomputation overlay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-visualization-and-parameter-controls/04-CONTEXT.md
@.planning/phases/04-advanced-visualization-and-parameter-controls/04-RESEARCH.md
@.planning/phases/04-advanced-visualization-and-parameter-controls/04-UI-SPEC.md
@.planning/phases/04-advanced-visualization-and-parameter-controls/04-01-SUMMARY.md
@.planning/phases/04-advanced-visualization-and-parameter-controls/04-02-SUMMARY.md

@frontend/src/stores/visualizationStore.ts
@frontend/src/components/sidebar/Sidebar.tsx
@frontend/src/components/sidebar/ControlSliders.tsx
@frontend/src/components/canvas/PointCloud.tsx
@frontend/src/components/canvas/ScatterCanvas.tsx
@frontend/src/App.tsx
@frontend/src/lib/heatmap.ts
@frontend/src/lib/plasma.ts
@frontend/src/hooks/usePersistenceImage.ts
@backend/api/routes/viz.py
@backend/worker/jobs.py

<interfaces>
<!-- Contracts from Plans 01 and 02 that this plan depends on -->

From frontend/src/stores/visualizationStore.ts (extended by Plan 01):
```typescript
activeTab: 'scatter' | 'topology' | 'compare'
selectedHomologyDim: 0 | 1 | 2
vrEpsilon: number
compareMode: boolean
compareGenre: string | null
settingsDrawerOpen: boolean
pipelineExplanationOpen: boolean
pipelineExplanationStep: number
isRecomputing: boolean
isRetraining: boolean
dirtyParams: Set<string>
h2Enabled: boolean
// ... plus setters for each
```

From frontend/src/lib/heatmap.ts (created by Plan 01):
```typescript
export function renderHeatmap(canvas: HTMLCanvasElement, data: number[], M: number, vmin: number, vmax: number): void
export function computeMinMax(data: number[]): { min: number; max: number }
```

From frontend/src/hooks/usePersistenceImage.ts (created by Plan 01):
```typescript
export function usePersistenceImage(genreOrBookId: string | null, dim: number, isBook: boolean): UseQueryResult<{data: number[], M: number, dim: number, vmin: number, vmax: number}>
```

From backend/worker/jobs.py:
```python
async def classify_book(ctx, file_content: bytes, job_id: str): ...
# Pattern: arq enqueue_job -> redis pub/sub -> WebSocket progress
```

PARAM_DEPENDENCY_MAP (from RESEARCH.md Pattern 3):
```python
PARAM_DEPENDENCY_MAP = {
    'grid_resolution': ['persistence_images', 'features', 'svm'],
    'sigma': ['persistence_images', 'features', 'svm'],
    'k_clusters': ['cluster_distribution', 'features', 'svm'],
    'alpha': ['features', 'svm'],
    'svm_C': ['svm'],
    'svm_gamma': ['svm'],
    'epsilon_max': ['homology', 'persistence_images', 'vr_edges', 'features', 'svm'],
    'vector_size': ['word2vec', 'tfidf', 'homology', 'persistence_images',
                    'cluster_distribution', 'features', 'svm', 'projections', 'vr_edges'],
    'window': ['word2vec', 'tfidf', 'homology', 'persistence_images',
               'cluster_distribution', 'features', 'svm', 'projections', 'vr_edges'],
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compare mode with dual brightness and stacked heatmaps</name>
  <files>
    frontend/src/components/compare/CompareControls.tsx,
    frontend/src/components/compare/CompareHeatmaps.tsx,
    frontend/src/components/sidebar/Sidebar.tsx,
    frontend/src/components/canvas/PointCloud.tsx,
    frontend/src/App.tsx
  </files>
  <behavior>
    - Test: CompareControls renders "Compare Genres" toggle button; clicking toggles compareMode in store
    - Test: when compareMode=true, second genre picker appears
    - Test: CompareHeatmaps computes shared vmin/vmax from two datasets (COMP-02)
    - Test: PointCloud in compare mode renders both genres at full opacity and dims everything else
  </behavior>
  <action>
1. **Create `CompareControls.tsx`** (per COMP-01, CONTEXT.md compare layout):
   - "Compare Genres" button: shadcn `Button` variant="outline" size="sm", icon lucide `Columns2` (16px).
   - Active state: `#6366F1` background, `#F5F5FF` text. Toggles `compareMode` in store.
   - When `compareMode=true`: render a second `GenreSelect` dropdown below the existing one with 8px gap. Label "Compare with" (12px, `#6B6B80`). On change: `setCompareGenre(genre)`.
   - When `compareMode=false`: hide second picker, clear `compareGenre`.

2. **Create `CompareHeatmaps.tsx`** (per COMP-01, COMP-02):
   - Two stacked persistence heatmaps in sidebar when compare mode is active and both genres selected.
   - Uses `usePersistenceImage` for both `selectedGenre` and `compareGenre`.
   - Computes SHARED vmin/vmax: `computeMinMax([...dataA.data, ...dataB.data])` from heatmap.ts.
   - Renders two `<canvas>` elements, each 260x260px, stacked with 16px gap.
   - Each labeled with genre name above (12px, genre color).
   - Both call `renderHeatmap` with the SAME vmin/vmax (per COMP-02 consistent color scale).
   - Axis labels at 11px (smaller than panel version).

3. **Modify `PointCloud.tsx`** for compare mode:
   - Read `compareMode` and `compareGenre` from store.
   - When `compareMode=true` and both genres selected:
     - Genre A points: full palette color, opacity based on TF-IDF.
     - Genre B points: full palette color (compareGenre's color), opacity based on TF-IDF fetched for compareGenre.
     - All other points: dim to 4% opacity.
   - Need to also fetch TF-IDF for compareGenre. Add a `useBookTfidf`/`useGenreTfidf` call in App.tsx for compareGenre and pass as `compareTfidfWeights` prop to PointCloud.

4. **Modify `Sidebar.tsx`**: Insert `<CompareControls />` below the genre dropdown. Insert `<CompareHeatmaps />` below compare controls when compareMode is active.

5. **Modify `App.tsx`**: When compareMode is active, fetch TF-IDF for compareGenre and pass both tfidfWeights arrays to ScatterCanvas/PointCloud. Wire compareMode tab behavior -- Compare tab in top nav activates compareMode + switches to scatter view.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - "Compare Genres" button toggles compare mode; second genre picker appears
    - Both genres render simultaneously in scatter with dual brightness
    - Two stacked heatmaps appear in sidebar with shared color scale (COMP-02)
    - Deactivating compare mode returns to single-genre view
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings drawer with tiered parameter controls and recomputation</name>
  <files>
    frontend/src/components/settings/SettingsDrawer.tsx,
    frontend/src/components/settings/SlowTierParams.tsx,
    frontend/src/components/settings/VerySlowTierParams.tsx,
    frontend/src/components/settings/RecomputeOverlay.tsx,
    frontend/src/hooks/useRecompute.ts,
    backend/api/routes/viz.py
  </files>
  <behavior>
    - Test: SettingsDrawer opens when settingsDrawerOpen=true in store
    - Test: changing a slow-tier param adds it to dirtyParams set; badge appears
    - Test: Recompute button is disabled when dirtyParams is empty
    - Test: VerySlowTierParams opens confirm dialog on change
    - Test: RecomputeOverlay renders when isRecomputing=true; canvas still receives pointer events
    - Test: POST /viz/recompute validates parameter ranges with Pydantic
    - Test: POST /viz/recompute with out-of-range values returns 422
  </behavior>
  <action>
1. **Create `SettingsDrawer.tsx`** (per PARAM-03, CONTEXT.md settings drawer):
   - shadcn `Sheet` (side="right"), triggered by `settingsDrawerOpen` in store.
   - Width 400px, background `#111118`, 1px left border `#1E1E2A`.
   - Header: "Settings" (18px heading) + close button (lucide `X`).
   - Two sections: `<SlowTierParams />` and `<VerySlowTierParams />`, separated by 1px `#1E1E2A` divider + 24px gap.
   - Close: Esc, click X, click overlay. overlay `rgba(0,0,0,0.5)`.
   - 300ms slide-in from right, ease-out.

2. **Create `SlowTierParams.tsx`** (per PARAM-03):
   - Section heading: "Recompute Parameters" (14px, 600 weight).
   - Description: "Changes require explicit recomputation. Current view remains interactive." (12px, `#6B6B80`).
   - 8 parameter sliders following ControlSliders.tsx pattern:
     | Parameter | Label | Range | Default | Step |
     |-----------|-------|-------|---------|------|
     | grid_resolution | "Persistence Image Resolution (M)" | 10-100 | 20 | 5 |
     | sigma | "Gaussian sigma" | 0.01-2.0 | 0.1 | 0.01 |
     | k_clusters | "Cluster Count (K)" | 10-200 | 50 | 5 |
     | alpha | "Feature Weight (alpha)" | 0.0-1.0 | 0.5 | 0.05 |
     | svm_gamma | "SVM gamma" | 0.001-10.0 | 1.0 | 0.01 |
     | svm_C | "SVM C" | 0.01-100.0 | 1.0 | 0.1 |
     | epsilon_max | "Epsilon Max" | 0.1-5.0 | 1.0 | 0.05 |
     | epsilon_step | "Epsilon Step Size" | 0.001-0.5 | 0.01 | 0.001 |
   - Track dirty state: each param compared to "last computed" value. When different, add to `dirtyParams` in store. When equal, remove.
   - Dirty badge: amber pill "Parameters changed -- Recompute Results" (bg `#FBBF24`, text `#111118`). Appears when dirtyParams.size > 0.
   - H2 toggle: "Enable H2 computation" checkbox below params. Toggles `h2Enabled` in store. When toggled on, adds 'h2' to dirtyParams.
   - Recompute button: full-width, 44px, accent bg `#6366F1`. Label "Recompute Results". Disabled when dirtyParams empty. Loading state: spinner + "Recomputing...". On click: calls `useRecompute` hook.

3. **Create `VerySlowTierParams.tsx`** (per PARAM-04):
   - Section heading: "Model Parameters" (14px, 600 weight).
   - Warning banner: bg `rgba(239,68,68,0.1)`, border 1px `#EF4444`, icon lucide `AlertTriangle`. Text: "Changing these will retrain the Word2Vec model. Estimated time: 2-5 minutes."
   - 2 parameter sliders: Embedding Dimension (50-300, default 150, step 10) and Context Window (2-15, default 5, step 1).
   - On ANY change: immediately open shadcn `Dialog` confirm:
     - Title: "Retrain Word2Vec Model?"
     - Body: "This will retrain the entire model with the new parameters. Current visualizations will be unavailable during retraining. Estimated time: 2-5 minutes."
     - Actions: "Keep Current Model" (outline, reverts value) + "Retrain Model" (destructive, `#EF4444` bg, triggers retrain).
   - On confirm: set `isRetraining=true` in store, trigger retrain job via useRecompute.

4. **Create `RecomputeOverlay.tsx`** (per PARAM-05):
   - Renders over canvas area (z-index 25) when `isRecomputing=true` or `isRetraining=true`.
   - Background `rgba(10,10,15,0.4)`.
   - Centered badge: bg `#1E1E2A`, border `#2A2A3A`, "Updating..." or "Retraining model..." with spinner.
   - CRITICAL: `pointer-events: none` on the overlay so orbit/pan/zoom still work on the canvas underneath. The badge itself can have `pointer-events: auto` if it needs a cancel button.
   - Minimum display time: 500ms (prevents flash).
   - For retrain: full overlay with cancel button. All controls disabled except cancel.

5. **Create `useRecompute.ts`**: Hook for triggering recomputation.
   - `useRecompute()` returns `{ triggerRecompute, triggerRetrain, cancelRecompute }`.
   - `triggerRecompute(changedParams: Record<string, number>)`:
     - POST to `/viz/recompute` with `{changed_params: {...}}`.
     - Backend determines affected subtree using PARAM_DEPENDENCY_MAP.
     - Set `isRecomputing=true` in store.
     - Listen on WebSocket for progress (same pattern as classify.py).
     - On completion: invalidate affected React Query caches, set `isRecomputing=false`, clear `dirtyParams`.
   - `triggerRetrain(params: {vector_size: number, window: number})`:
     - POST to `/viz/recompute` with very-slow params.
     - Set `isRetraining=true`.
     - On completion: invalidate ALL caches (full refresh), set `isRetraining=false`.

6. **Add `POST /viz/recompute` endpoint** to `backend/api/routes/viz.py` (per PARAM-06):
   - Pydantic model for request body with strict field validation and range constraints:
     ```python
     class RecomputeRequest(BaseModel):
         changed_params: dict[str, float]
         class Config:
             extra = 'forbid'
     ```
   - Validate each param name against known set. Validate each value against its range.
   - Determine affected pipeline steps using PARAM_DEPENDENCY_MAP.
   - Enqueue arq job with affected steps list.
   - Return `{job_id: str, affected_steps: list[str]}`.
   - Rate limit: reject if `isRecomputing` flag is set (only 1 concurrent recompute per session).

7. **Wire `RecomputeOverlay` into `App.tsx`**: Render above canvas area when `isRecomputing` or `isRetraining` is true.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose && cd .. && python -m pytest backend/tests/ -x -q</automated>
  </verify>
  <done>
    - Settings drawer opens via gear icon, shows slow-tier and very-slow-tier params
    - Changing a slow-tier param shows amber dirty badge; Recompute button enables
    - Clicking Recompute triggers backend job; overlay appears; canvas stays interactive
    - Changing very-slow param opens confirm dialog with warning
    - Confirming retrain shows full overlay with "Retraining model..."
    - POST /viz/recompute validates params and returns affected steps
    - After recompute completes: overlay disappears, data refreshes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pipeline explanation, export, and final wiring</name>
  <files>
    frontend/src/components/explanation/PipelineExplanation.tsx,
    frontend/src/components/explanation/steps/Step1WordEmbed.tsx,
    frontend/src/components/explanation/steps/Step2TfidfWeight.tsx,
    frontend/src/components/explanation/steps/Step3PointCloud.tsx,
    frontend/src/components/explanation/steps/Step4Homology.tsx,
    frontend/src/components/explanation/steps/Step5PersistenceImage.tsx,
    frontend/src/components/explanation/steps/Step6Classification.tsx,
    frontend/src/lib/exportUtils.ts,
    frontend/src/lib/exportUtils.test.ts,
    frontend/src/components/topology/PersistenceHeatmap.tsx,
    frontend/src/components/sidebar/Sidebar.tsx
  </files>
  <behavior>
    - Test: exportScatterPNG calls canvas.toDataURL and creates download link
    - Test: exportPersistenceCSV generates correct CSV header and rows from diagram data
    - Test: PipelineExplanation renders 6 steps with correct titles
    - Test: keyboard left/right navigates between steps; Esc closes
    - Test: step indicator shows "Step N / 6" correctly
  </behavior>
  <action>
1. **Create `PipelineExplanation.tsx`** (per EXPLAIN-01, CONTEXT.md pipeline explanation):
   - shadcn `Dialog` (fullscreen variant). Triggered by `pipelineExplanationOpen` in store.
   - Overlay: `rgba(10,10,15,0.95)`.
   - Content: max-width 960px centered, bg `#111118`, border-radius 12px, padding 32px.
   - Step indicator: "Step {n} / 6" (12px, `#6B6B80`, top-right).
   - Navigation: "Previous" (outline, disabled on step 1) + 6 step dots (8px, active=`#6366F1`, inactive=`#2A2A3A`) + "Next" (outline, becomes "Close" variant="default" on step 6).
   - Keyboard: Left arrow = previous, Right arrow = next, Esc = close.
   - Content crossfade 200ms between steps (opacity transition).
   - Reads `pipelineExplanationStep` from store, sets via `setPipelineExplanationStep`.
   - Close: X button, Esc, "Close" on final step. Sets `pipelineExplanationOpen=false` and resets step to 0.

2. **Create 6 step components** (per EXPLAIN-01):
   - `Step1WordEmbed.tsx`: Title "Word2Vec Embedding". Visual: simplified scatter of ~20 labeled words in a static mini 3D view. Text explains how words become vectors.
   - `Step2TfidfWeight.tsx`: Title "TF-IDF Weighting". Visual: before/after comparison -- two side-by-side word clouds, one uniform brightness, one with TF-IDF brightness applied. Text explains importance weighting.
   - `Step3PointCloud.tsx`: Title "Weighted Point Cloud". Visual: word cloud with sized points (radius proportional to TF-IDF). Genre-colored clusters visible. Text explains how genres form spatial patterns.
   - `Step4Homology.tsx`: Title "Persistent Homology". Visual: MINI interactive VR animation with ~50 points. Reuse EpsilonSlider + simplified VR edge rendering. User can drag slider. Text explains how features are born/die during filtration.
   - `Step5PersistenceImage.tsx`: Title "Persistence Image". Visual: heatmap of current book/genre persistence image (reuse PersistenceHeatmap component). Text explains how the diagram becomes a fixed-length vector.
   - `Step6Classification.tsx`: Title "SVM Classification". Visual: bar chart of genre confidence scores (use simple div bars, no chart library). Text explains kernel SVM on concatenated features.
   - Each step uses the currently selected book/genre data when available; falls back to genre average or corpus average if nothing selected.

3. **Create `exportUtils.ts`** (per UX-03):
   - `exportScatterPNG(canvas: HTMLCanvasElement, genre: string, projection: string)`:
     - `canvas.toDataURL('image/png')`.
     - Create temp anchor with download attribute: `lgt-scatter-{genre}-{projection}-{timestamp}.png`.
     - Click anchor to trigger download.
   - `exportHeatmapPNG(canvas: HTMLCanvasElement, genre: string, dim: number)`:
     - Same pattern. Filename: `lgt-persistence-{genre}-H{dim}-{timestamp}.png`.
   - `exportPersistenceCSV(diagrams: {birth: number, death: number, dimension: number}[], genre: string, dim: number)`:
     - Header: `birth,death,dimension,persistence\n`.
     - Each row: `${d.birth},${d.death},${d.dimension},${d.death - d.birth}`.
     - Create Blob, URL.createObjectURL, temp anchor download.
     - Filename: `lgt-persistence-{genre}-H{dim}-{timestamp}.csv`.

4. **Create `exportUtils.test.ts`**: Test export functions.
   - Mock canvas.toDataURL to return 'data:image/png;base64,...'.
   - Assert anchor element created with correct filename pattern.
   - Test CSV generation: verify header + data rows for known input.

5. **Wire export buttons**:
   - `PersistenceHeatmap.tsx`: add "PNG" and "CSV" buttons in panel header. PNG calls `exportHeatmapPNG` with the heatmap canvas ref. CSV calls `exportPersistenceCSV` with current persistence data.
   - `Sidebar.tsx`: add "Export PNG" button in sidebar footer. Calls `exportScatterPNG` with the scatter canvas ref (requires passing canvas ref from ScatterCanvas up through props or via a ref in the store/context).
   - Button feedback: show check icon for 2s after click, then revert.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/exportUtils.test.ts --reporter=verbose && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - "How It Works" button opens 6-step pipeline explanation dialog
    - Steps navigate with left/right arrows and Prev/Next buttons
    - Step 4 has mini interactive VR animation
    - Esc closes the dialog and returns to previous state
    - Export PNG button in sidebar triggers scatter canvas download
    - Export PNG/CSV buttons on persistence panel trigger heatmap/CSV downloads
    - exportUtils.test.ts passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of complete Phase 4 feature set</name>
  <files>none</files>
  <action>Human verifies all Phase 4 features visually in the running application.</action>
  <what-built>
    Complete Phase 4 feature set: top navigation tabs, topology tab (persistence heatmap + VR viewer), genre comparison mode, settings drawer with tiered recomputation, pipeline explanation dialog, export functionality, and topology disclaimer.
  </what-built>
  <how-to-verify>
    1. Open the app at http://localhost:5173
    2. **Tab navigation**: Click Scatter / Topology / Compare tabs -- each should show its respective view. Disclaimer banner should appear on Scatter and Topology tabs.
    3. **Topology tab**: Select a genre. Left panel should show persistence image heatmap with plasma colors. Switch H0/H1 tabs. H2 should be disabled.
    4. **VR viewer**: In Topology tab, drag the epsilon slider. Edges should appear progressively. Newly born edges should flash yellow (#FACC15) briefly. Orbit/pan/zoom should work independently.
    5. **Compare mode**: Click "Compare Genres" in sidebar. Select a second genre. Both genres should render in scatter with dual brightness. Two stacked heatmaps should appear in sidebar with consistent color scale.
    6. **Settings drawer**: Click gear icon. Drawer slides in. Change a parameter (e.g., sigma). Amber badge should appear. Click "Recompute Results". Overlay should appear on canvas ("Updating..."). Canvas should still be orbitable behind overlay.
    7. **Very-slow params**: In settings drawer, change "Embedding Dimension". Confirm dialog should appear with warning. Click "Keep Current Model" to cancel.
    8. **Pipeline explanation**: Click "How It Works". 6-step slide deck should appear. Navigate with arrows. Step 4 should have interactive mini VR. Esc should close.
    9. **Export**: Click "Export PNG" in sidebar -- should download a PNG of the scatter. In Topology tab, click PNG/CSV buttons on heatmap panel -- should download files.
  </how-to-verify>
  <verify>Human visual verification -- see how-to-verify steps above</verify>
  <done>All 9 verification steps pass; user types "approved"</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> POST /viz/recompute | User-submitted parameter values for recomputation |
| client -> export functions | Canvas data exported to user's filesystem (no server involvement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-07 | Tampering | POST /viz/recompute | mitigate | Pydantic model with `extra='forbid'`; validate param names against known set; range constraints on all numeric values |
| T-4-08 | Denial of Service | POST /viz/recompute (slow-tier) | mitigate | Reject if concurrent recompute in progress; arq job timeout; disable button during computation |
| T-4-09 | Denial of Service | POST /viz/recompute (very-slow) | mitigate | Confirm dialog on frontend prevents accidental trigger; arq job timeout (10 min); cancel support via cancel_event |
| T-4-10 | Information Disclosure | export functions | accept | Exports only data the user is already viewing; no auth; public app |
</threat_model>

<verification>
1. `cd frontend && npx vitest run --reporter=verbose` -- all frontend tests green
2. `cd backend && python -m pytest -x -q` -- all backend tests green
3. All 16 Phase 4 requirements verified through manual checkpoint
4. Tab navigation, topology views, compare mode, settings, explanation, and export all functional
</verification>

<success_criteria>
- Compare mode: dual brightness in scatter + stacked heatmaps with consistent color scale (COMP-01, COMP-02)
- Settings drawer: slow-tier params with dirty badge + Recompute button (PARAM-03)
- Very-slow params: warning banner + confirm dialog (PARAM-04)
- Recompute overlay: canvas interactive, "Updating..." badge visible (PARAM-05)
- Selective recomputation: backend determines affected subtree (PARAM-06)
- Pipeline explanation: 6-step dialog with keyboard nav (EXPLAIN-01)
- Export: PNG for scatter and heatmap, CSV for persistence data (UX-03)
- Disclaimer banner visible on Scatter/Topology tabs (UX-05)
- Human verification checkpoint passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-visualization-and-parameter-controls/04-03-SUMMARY.md`
</output>
