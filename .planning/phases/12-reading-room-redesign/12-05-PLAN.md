---
phase: 12
plan: 12-05
title: Topology screen — reading-room skin (R3F VR hero + ε slider + diagram + image)
wave: 3
autonomous: true
estimated_hours: 3-5
depends_on: [12-01]
files_modified:
  - frontend/src/components/screens/Topology.tsx
  - frontend/src/components/topology/TopologyPanel.tsx
  - frontend/src/components/topology/VRViewer.tsx
  - frontend/src/components/topology/EpsilonSlider.tsx
  - frontend/src/components/topology/PersistenceDiagram.tsx
  - frontend/src/components/topology/PersistenceHeatmap.tsx
  - frontend/src/App.tsx
requirement_ids: [RR-06]
---

# Plan 12-05 — Topology screen (reading-room skin)

## Objective
Re-skin the existing Topology tab into the reading-room idiom (§6.4 / L-11). Header: title "The topology of *{Region}*" + Region chip row + 3D-projection chips. Hero left (`1.5fr`): "i · Vietoris–Rips filtration" framed R3F viewer (existing `VRViewer`: drag-orbit, ring nodes tinted with the **reading-room genre hex**, edges appear as ε grows, freshly-born edges flash the **accent**) + readout "{n} edges · {k} loop(s) alive" + ε slider (**accent-filled** track, not amber) + caption. Side right (`300px`): "ii · Persistence diagram (H₁)" (birth×death; diagonal; finite dots ∝ √persistence; ∞ marker; **accent sweep lines + shaded alive corner at ε**) and "iii · Persistence image (20×20 → 400-vector)" (heatmap with **`paper2 → genreHex → ink`** ramp + density legend). Empty state until a region is selected. N-D disclaimer footer.

Builds on the existing topology components + hooks (`useVRData`, `usePersistenceDiagram`, `usePersistenceImage`) — reskin + add the ε→diagram sweep/alive-shading and ε→image guide if not already present. H₁ only. Projection changes only the 3D viewer (L-11/L-13... L-11). Screenshot: `04-topology.png`. Prototype: `screens_topology.jsx`.

## Tasks (atomic commits, in order)

### Task 1 — Reskin VR hero + ε slider + readout
- `VRViewer.tsx`: scene bg → `paper`/scene token (imperative, no remount); ring/structural nodes → reading-room genre hex; freshly-born edges flash **accent** (not amber) over ~500ms; resting edges = `ink`-ish hairlines.
- `EpsilonSlider.tsx`: filled track → **accent** (replace the `#FACC15` literal); keep store-driven + edge count + 3-decimal readout.
- Add the "{n} edges · {k} loop(s) alive" readout (singularise at k=1) to the VR panel header.
- Commit: `feat(12-05): reading-room VR hero — paper scene, accent edges/slider, loops-alive readout`

### Task 2 — Diagram + image reskin + ε links
- `PersistenceDiagram.tsx`: accent sweep lines at ε (vertical+horizontal) + shaded alive corner (`birth≤ε≤death`); alive dots opaque + `ink` outline, others dimmed; finite dots ∝ √persistence; ∞ strip (filter `Infinity` before axis bounds). Recolor to reading-room (accent + ink + paper).
- `PersistenceHeatmap.tsx`: heatmap ramp → `paper2 → genreHex → ink` (replace PLASMA); density legend; ε birth-axis guide.
- Commit: `feat(12-05): reading-room persistence diagram + image with ε links`

### Task 3 — Topology screen composition + header
- `components/screens/Topology.tsx` + re-laid `TopologyPanel.tsx`: header (title + Region chips + projection chips), hero (1.5fr) + side column (300px), empty state ("Pick a region to see its topology."), N-D disclaimer footer. `data-tour-id="topology-plate"` on the VR viewer (for the tour). Route registered.
- Commit: `feat(12-05): topology screen — header, hero+side layout, empty state, disclaimer`

### Task 4 — Verify + SUMMARY
- tsc clean; Playwright tour-anchor smoke updated for `topology-plate`; manual vs `04-topology.png` (ε scrub links all three; accent signal; genre-hex heatmap; projection changes only 3D).
- `12-05-SUMMARY.md`; STATE; `roadmap update-plan-progress 12 12-05 completed`.
- Commit: `docs(12-05): complete topology reskin plan`

## Success criteria
- [ ] Matches `04-topology.png`: VR hero + accent ε slider + loops-alive readout; diagram with accent sweep + alive corner + √ dots + ∞ strip; image with paper2→genreHex→ink ramp + legend
- [ ] ε links all three; projection changes only the 3D viewer; H₁ only; N-D disclaimer present
- [ ] `Infinity` filtered before axis bounds; scene recolors live (no remount)
- [ ] `data-tour-id="topology-plate"` present; tsc clean; SUMMARY + STATE/ROADMAP updated

## Deviation rules
- README/L-11 win; reading-room uses **accent** (not amber `#FACC15`) for ε and **genre-hex** heatmap ramp.
- Reuse the existing topology hooks/components; don't change backend math.
