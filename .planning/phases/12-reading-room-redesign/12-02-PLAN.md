---
phase: 12
plan: 12-02
title: Collection — catalog rail + reskinned R3F plate + marginalia + region filter + Find
wave: 2
autonomous: true
estimated_hours: 4-6
depends_on: [12-01]
files_modified:
  - frontend/src/components/screens/Collection.tsx
  - frontend/src/components/collection/CatalogRail.tsx
  - frontend/src/components/collection/Marginalia.tsx
  - frontend/src/components/collection/PlateFrame.tsx
  - frontend/src/components/canvas/ScatterCanvas.tsx
  - frontend/src/components/canvas/PointCloud.tsx
  - frontend/src/App.tsx
requirement_ids: [RR-02]
---

# Plan 12-02 — Collection screen

## Objective
Build the Collection screen (§6.2): 3-column carrel (catalog rail 260 / plate 1fr / marginalia 300), collapsing to 2-col under `study` density. The plate is the **reskinned existing R3F scatter** (D-U1) — NOT SVG — wrapped in a reading-room `PlateFrame` (corner rulings "Plate I", "{proj} · {dim} · ε 0.42", fig. caption with footnote¹). Region filter dims non-selected points to ~0.15; hover → marginalia note + floating tooltip; click → routes to catalog card.

Spec: README §6.2, §7 (hover/click/region filter), §10 (responsive). Prototype: `screens_collection.jsx` (CorpusScatter), `shared.jsx`. Screenshot: `02-collection.png`. Data: `useScatterData`, `useCorpusBooks`. Tour anchors: `data-tour-id="catalog-rail"`, `"plate"`.

## Tasks (atomic commits, in order)

### Task 1 — Reskin the R3F plate (ScatterCanvas/PointCloud) to reading-room
- Scene background → `paper` (read CSS var imperatively, no canvas remount — keep the Phase 10 imperative pattern but point it at `--paper`/the reading-room scene token).
- Point colors → the 8 reading-room genre hexes (L-05) via `GENRE_COLORS`.
- Region filter: non-selected genre dims to ~0.15 opacity (reuse the existing genre-dim path; just confirm thresholds).
- Keep hover/click/selection-ring, projection switching, 2D/3D toggle behaviour.
- Commit: `feat(12-02): reskin R3F scatter to reading-room paper + genre hexes`

### Task 2 — PlateFrame + projection chips + 2D/3D
- `components/collection/PlateFrame.tsx`: the framed figure wrapper — 1px ink border, corner rulings (top-left "Plate I", top-right "{proj} · {dim} · ε 0.42"), `fig. 1 — …` caption with footnote¹, "↻ recompute / ⧉ export" affordances per screenshot. Houses the R3F canvas.
- Projection chips (`PCA KPCA UMAP t-SNE`) + `2D/3D` toggle wired to `visualizationStore` (reuse existing state). 3D = the real R3F 3D (per D-U1), not a CSS tilt.
- Commit: `feat(12-02): reading-room plate frame + projection/2D-3D controls`

### Task 3 — Catalog rail + Find + Marginalia + compose Collection
- `components/collection/CatalogRail.tsx`: "Card catalog" header, "All regions {n}", 8 genres each with mono index numeral + color dot + count; click filters (accent left-border); expanding a genre lists its titles → click opens that book's card. A "Find" search field (word/title). On `useCorpusBooks`.
- `components/collection/Marginalia.tsx`: hovered book's note (title, author·year, region dot, driving-word chips, "open catalog card →") else a prompt; plus the standing UMAP-distortion note.
- `components/screens/Collection.tsx`: 3-col grid (carrel) / 2-col (study, marginalia hidden); wire hover→`hoveredBookId`, click→`selectedBookId`+route `card`. Add `data-tour-id="catalog-rail"` + `"plate"`.
- Register the route in `App.tsx`.
- Commit: `feat(12-02): catalog rail + Find + marginalia + Collection composition`

### Task 4 — Verify + SUMMARY
- tsc clean; suite runnable; manual vs `02-collection.png` (rail filters, hover marginalia+tooltip, click→card route, projection/2D-3D, study density hides marginalia).
- `12-02-SUMMARY.md`; STATE; `roadmap update-plan-progress 12 12-02 completed`.
- Commit: `docs(12-02): complete Collection plan`

## Success criteria
- [ ] 3-col carrel (rail/plate/marginalia); 2-col under study density
- [ ] Plate = reskinned R3F scatter (paper bg, genre hexes), framed with corner rulings + caption¹
- [ ] Region filter dims non-selected to ~0.15; rail click filters + expands titles
- [ ] Hover → marginalia note + tooltip; click → catalog card route
- [ ] Projection chips + 2D/3D wired; Find search works
- [ ] `data-tour-id="catalog-rail"` + `"plate"` present
- [ ] tsc clean; SUMMARY + STATE/ROADMAP updated

## Deviation rules
- README wins over prototype; D-U1 (R3F plate, not SVG) applies.
- Don't add a second WebGL canvas for marginalia/thumbnails — those stay SVG/static.
- Reuse `useScatterData`/`useCorpusBooks` as source of truth; the prototype's `data.js` is layout-only.
