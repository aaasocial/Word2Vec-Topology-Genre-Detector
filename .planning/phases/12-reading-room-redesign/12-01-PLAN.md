---
phase: 12
plan: 12-01
title: Foundation — reading-room tokens, type system, shell (masthead/footer/footnotes), Tweaks, router, Landing + About
wave: 1
autonomous: true
estimated_hours: 4-6
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/constants/genres.ts
  - frontend/src/theme/readingRoom.ts
  - frontend/src/stores/readingRoomStore.ts
  - frontend/src/components/shell/Masthead.tsx
  - frontend/src/components/shell/Footer.tsx
  - frontend/src/components/shell/FootnoteHost.tsx
  - frontend/src/components/shell/TweaksPanel.tsx
  - frontend/src/components/screens/Landing.tsx
  - frontend/src/components/screens/About.tsx
requirement_ids: [RR-01]
---

# Plan 12-01 — Foundation: tokens, shell, Tweaks, router, Landing + About

## Objective

Stand up the reading-room visual system and app shell that every other screen builds on, and prove it with the two simplest screens (Landing + About). Replace the Phase 10 indigo `index.css` system and the Phase 11 tabbed-app `App.tsx` shell wholesale (per D-U2). After this plan the app boots into the Reading Room masthead with a working Landing page, a Guide button stub, and live Tweaks (paper/accent/density) — even though most masthead routes are still placeholders.

Authoritative spec: `design_handoff/design_handoff_reading_room/README.md` §6.1 (Landing), §6.8 (About), §6.11 (Tweaks), §5 + `tokens.md` (tokens), §4 L-01..L-07/L-14, §7 (state), §10 (responsive). Prototype: `shell.jsx`, `app.jsx`, `screens_landing.jsx`. Screenshots: `01-landing.png`, `08-about.png`.

## Tasks (atomic commits, in order)

### Task 1 — Fonts + type system + reading-room tokens
- `frontend/index.html`: replace any Phase 10/11 inline theme script + fonts with the Spectral + JetBrains Mono Google Fonts import from tokens.md (`Spectral:ital,wght@0,300..700;1,400;1,500` + `JetBrains+Mono:wght@400;500;600`). Remove the Phase 11 light-default FOUC script (the reading room uses paper palettes, not `.light`).
- `frontend/src/index.css`: **replace** the indigo `:root`/`:root.light` HSL block with the reading-room system as CSS custom properties driven by the active palette+accent: `--paper`, `--paper2`, `--card`, `--ink`, `--muted`, `--accent`, plus type-scale helpers and rule/shadow tokens (hairline, masthead rule, catalog-card `border-top:4px double` + `4px 4px 0 {ink}22` hard shadow, block shadow `6px 6px 0 {ink}33`, radius 0). Default palette `cream`, default accent `oxblood`. Body font Spectral; mono JetBrains Mono.
- `frontend/src/theme/readingRoom.ts`: the 4 palettes + 4 accents as typed objects (mirror `RR_PALETTES`/`RR_ACCENTS` from `shell.jsx`) + an `applyReadingRoomTheme(palette, accent)` that writes the CSS vars onto `<html>`.
- `frontend/src/constants/genres.ts`: set the 8 genre hexes to the reading-room values (L-05); update/verify every consumer compiles.
- Commit: `feat(12-01): reading-room type system + paper/accent/genre tokens (replaces indigo)`

### Task 2 — Reading-room store (route + ui + tweaks)
- `frontend/src/stores/readingRoomStore.ts` (or extend `uiStore`): `route` ('landing'|'collection'|'card'|'topology'|'study'|'upload'|'verdict'|'about'), `guideOpen`, `guideSeen` (persisted localStorage `rr.guide.seen.v1`), `tweaks: { paper, accent, density }` (persisted), `tourActive`/`tourStep`, plus actions. Keep `visualizationStore` (genreFilter, hovered/selectedBookId, studyA/studyB, projection, dim) and `uploadStore` as the data-side stores — wire route/selection through them per §7. Pick the cleanest split (Claude's discretion per CONTEXT).
- On store init, apply persisted tweaks via `applyReadingRoomTheme`.
- Commit: `feat(12-01): reading-room store — route, guide, tweaks (persisted)`

### Task 3 — Masthead + Footer + FootnoteHost
- `components/shell/Masthead.tsx` (L-04): wordmark "Literary Genre Topology" + items (The Collection · Topology · A Comparative Study · Submit a Text · About) + a "Guide" button (opens the side-sheet — stub if Guide not built yet). Active item: accent underline + ink + roman; others italic + muted. Sticky top bar with `2px solid ink` rule.
- `components/shell/Footer.tsx`: the running footer ("A working library · est. 2026" / "edited by…" / page marker) per screenshots.
- `components/shell/FootnoteHost.tsx` (L-06): centered modal note opened by inline accent superscripts; the 6 FOOTNOTES copy verbatim from `app.jsx`; backdrop/Esc closes; block shadow `6px 6px 0 {ink}33`.
- Commit: `feat(12-01): masthead nav + footer + footnote host`

### Task 4 — Tweaks panel
- `components/shell/TweaksPanel.tsx` (§6.11): "Tweaks" — Warmth (4 paper swatches), Mark (4 accent swatches), Layout (carrel/study). Writes to the store + `applyReadingRoomTheme`; persisted. Hidden unless toggled on (a small toolbar control). Default cream/oxblood/carrel.
- Commit: `feat(12-01): Tweaks panel — paper/accent/density, persisted`

### Task 5 — App shell + router + Landing + About
- `frontend/src/App.tsx`: **replace** the Phase 10/11 tabbed shell + onboarding orchestrator with: Masthead + a route switch rendering the active screen + Footer + FootnoteHost + (Guide/tour mount points, stubbed if not built). Remove the Phase 11 How-It-Works→tour auto-chain. Fluid editorial layout (L-14/§10) — NOT the fixed 1240×780 artboard.
- `components/screens/Landing.tsx` (§6.1): two-column — left editorial intro (label, 64px display H1 "A library of *122 novels,* arranged by what they *say.*", two lede paragraphs, CTAs "Enter the reading room →" → collection / "Submit a text" → upload, 4-up stat row 122/8/12,808/UMAP); right a framed plate preview. Copy verbatim. The plate preview may use a static SVG thumbnail or the reskinned scatter (Claude's discretion — keep it cheap; the live R3F plate lands in 12-02).
- `components/screens/About.tsx` (§6.8): 2-col prose "On the method" / "On the genres", copy verbatim from `screens_landing.jsx`.
- Verify the app boots to Landing; masthead routes to placeholder screens + working About; Tweaks change paper/accent/density live and persist across reload.
- Commit: `feat(12-01): app shell + masthead router + Landing + About`

### Task 6 — Verify + SUMMARY
- `cd frontend && npx tsc --noEmit` → 0 (fix fallout from replaced theme/genre tokens across surviving components; some Phase 9/10 component tests may now reference removed styles — update or remove as needed, but keep the suite runnable).
- `npm run test -- --run` → green for surviving components (Phase 9 deferred failures excepted; document any tests removed because their component was replaced).
- Manual: app boots into Reading Room; Landing renders per `01-landing.png`; About per `08-about.png`; Tweaks work + persist.
- Write `12-01-SUMMARY.md`; update STATE.md; `roadmap update-plan-progress 12 12-01 completed`.
- Commit: `docs(12-01): complete foundation plan`

## Success criteria
- [ ] App boots into the reading-room masthead (no indigo theme remains in the live UI)
- [ ] Spectral + JetBrains Mono loaded; tokens.md type scale + cream/oxblood defaults applied
- [ ] Tweaks (paper/accent/density) change the look live and persist (localStorage)
- [ ] Masthead nav routes between screens; active-item styling per L-04
- [ ] Footnote superscripts open the centered modal note (6 notes, verbatim copy)
- [ ] Landing matches `01-landing.png`; About matches `08-about.png` (copy verbatim)
- [ ] Genre hexes = reading-room values (L-05) wherever genres render
- [ ] tsc clean; surviving test suite runnable; SUMMARY + STATE/ROADMAP updated

## Deviation rules
- **Rule 1:** README/tokens.md win over the prototype on any conflict.
- **Rule 2 (NOT-list §11):** no indigo theme anywhere; don't ship the fixed artboard; don't change backend/data.
- **Rule 3:** test churn from replaced components is expected — update/remove tests for replaced components; never delete a data hook or break a surviving one. Phase 9 deferred failures stay deferred.
- **D-U1/D-U2** from CONTEXT apply (R3F-reskin plates; full replacement).
