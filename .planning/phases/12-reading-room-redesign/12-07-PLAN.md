---
phase: 12
plan: 12-07
title: Responsive pass + animation-robustness pass + final verification
wave: 4
autonomous: true
estimated_hours: 2-4
depends_on: [12-01, 12-02, 12-03, 12-04, 12-05, 12-06]
files_modified:
  - frontend/src/index.css
  - frontend/src/components/screens/Collection.tsx
  - frontend/src/components/screens/Card.tsx
  - frontend/src/components/screens/Study.tsx
  - frontend/src/components/screens/Verdict.tsx
  - frontend/src/components/guide/Guide.tsx
  - frontend/src/tour/TourOverlay.tsx
requirement_ids: [RR-09]
---

# Plan 12-07 — Responsive + animation-robustness + final verification

## Objective
Make the fixed-artboard demo a genuinely fluid editorial layout (README §10) and harden every "alive" figure/animation to be background-tab safe (§7), then run the full phase verification.

## Tasks (atomic commits, in order)

### Task 1 — Responsive pass (§10)
- Masthead = normal sticky top bar (already). Collection/Card/Study/Verdict CSS-grid columns (`260/1fr/300` etc.) collapse: at <~1100px drop marginalia / sibling rail (reuse the `study` density 2-col fallback); at <~768px stack to one column in source order; plate/figures stay square + centered, min/max-clamped (respect existing `HEATMAP_SIZE` clamps for topology). Guide side-sheet → full-width on narrow; tour card clamps to viewport with the same quadrant logic.
- Commit: `feat(12-07): fluid responsive editorial layout (collapse at ~1100 / ~768)`

### Task 2 — Animation-robustness pass (§7 / L-08)
- Audit every "alive" element (How-it-works figures, VR birth-fade, FigTopology auto-sweep, probability-bar fills, centroid pulse): ensure each degrades to a valid static frame if the timeline pauses (background tab) — no content hidden behind `opacity:0 → forwards` entrance animations; drive scrub-critical fades with rAF, not CSS transitions.
- Commit: `fix(12-07): animation-robustness — figures/fades degrade to static frames`

### Task 3 — Final verification + SUMMARY
- `cd frontend && npx tsc --noEmit` → 0.
- `npm run test -- --run` → green for all surviving/updated components (Phase 9 deferred failures excepted; note any tests retired with replaced components).
- `npx playwright test` → tour-anchor smoke (6 stops) green.
- Manual sweep across all 8 routes + Guide + tour + Tweaks at wide / ~1000px / ~700px; cross-check each against its screenshot; confirm no indigo theme remains; confirm "marginal" voice; confirm topology H₁-only + N-D disclaimer.
- Write `12-07-SUMMARY.md`; update STATE.md; `roadmap update-plan-progress 12 12-07 completed`; flip RR-01..RR-09 to `[x]` in REQUIREMENTS.md with the Phase 12 reference; evolve PROJECT.md (front end recast as Reading Room; Phase 10/11 UI superseded).
- Commit: `docs(12-07): complete responsive + robustness pass; close Phase 12`

## Success criteria
- [ ] Layouts collapse cleanly at ~1100px (drop marginalia/rail) and ~768px (single column); Guide full-width + tour card clamped on narrow
- [ ] Every alive figure/animation degrades to a valid static frame (background-tab safe)
- [ ] tsc clean; Vitest green (surviving/updated); Playwright 6-stop tour-anchor smoke green
- [ ] All 8 routes + Guide + tour + Tweaks verified against screenshots at 3 widths; no indigo remains
- [ ] RR-01..RR-09 flipped to [x]; PROJECT.md evolved; SUMMARY + STATE/ROADMAP updated

## Deviation rules
- README §10/§7 win; don't ship the fixed artboard.
- Phase 9 deferred failures stay deferred; retiring tests for replaced components is expected (document them).
