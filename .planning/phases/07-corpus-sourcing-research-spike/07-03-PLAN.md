---
phase: 07-corpus-sourcing-research-spike
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v2/corpus_candidates.yaml
  - .planning/research/v2/_drafts/05_author_distribution_audit.md
autonomous: true
requirements: [RES-01]
tags: [research, corpus, gutenberg]

must_haves:
  truths:
    - "Sibling YAML file `corpus_candidates.yaml` exists in `.planning/research/v2/` with per-genre candidate-title shortlists"
    - "Each of the 10 v1 genres has a candidate list of ≥50 gutenberg_id entries per D-19"
    - "Each candidate entry contains gutenberg_id (integer), title (string), author (string), and source_consensus_score (integer count of curation sources)"
    - "Author distribution audit fragment documents each genre's distinct-author count and confirms whether ≥8 authors per genre (D-08) is satisfied"
    - "Audit fragment explicitly notes that D-07 (no per-author cap within a genre) is honored — no candidate list artificially caps prolific authors"
    - "Audit fragment includes a single explicit per-genre book-count number per D-06 (either 25 or 30, consistent with Plan 02's genre-set recommendation)"
    - "Audit fragment includes the D-10 books.yaml schema specification (sample YAML block showing the new source field)"
    - "Per-genre candidates are ranked by source_consensus_score descending (highest score = most curation sources agree this title belongs in this genre)"
  artifacts:
    - path: ".planning/research/v2/corpus_candidates.yaml"
      provides: "Per-genre candidate-title shortlists (50+ gutenberg_ids each) for Phase 8 to draw from when assembling the v2 corpus per D-19"
      contains: "genres:"
      min_lines: 550
    - path: ".planning/research/v2/_drafts/05_author_distribution_audit.md"
      provides: "Author-distribution audit confirming ≥8 authors per genre (D-08), no per-author cap (D-07), book counts per genre (D-06), and books.yaml schema additions (D-10)"
      contains: "## Author distribution audit"
      min_lines: 80
  key_links:
    - from: ".planning/research/v2/_drafts/05_author_distribution_audit.md"
      to: ".planning/research/v2/corpus_candidates.yaml"
      via: "Audit reads the YAML and counts distinct authors per genre"
      pattern: "corpus_candidates\\.yaml"
    - from: ".planning/research/v2/corpus_candidates.yaml"
      to: "gutenberg.org / Standard Ebooks / canon lists"
      via: "source_consensus_score is the count of curation lists each title appears on"
      pattern: "source_consensus_score|consensus"
---

<objective>
Produce the per-genre candidate-title shortlist (sibling YAML) and author-distribution audit (markdown fragment) that Plan 05 assembly will reference. This is the "give Phase 8 a pre-vetted pool to pick from" deliverable per D-19. Phase 8 still makes the final 25 or 30 selections, but from a list of ≥50 candidates per genre that already passed source-consensus + author-diversity + public-domain checks.

Per D-06, target per-genre book count is a single number (25 if Proposal C wins from Plan 02 → 10 genres; 30 if Proposal A or B wins → 8 genres). Per D-07, no per-author cap inside a genre. Per D-08, ≥8 distinct authors per genre is a hard constraint. Per D-10, the new `source: {provider, fetched_at, text_sha256}` field schema is specified here for Phase 8 to apply.

Purpose: Convert the "where do we look?" curation-pipeline finding from Plan 01 into actual gutenberg_ids Phase 8 can grep through. The per-author smoke test (D-17) is the only remaining anti-leakage guard given D-07's no-cap rule, so the audit must also document the author distribution honestly — including any genre where one author dominates the candidate list.

Output: One YAML file (`corpus_candidates.yaml`) sibling to the eventual `CORPUS_SOURCING.md`, plus one markdown fragment in `_drafts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-corpus-sourcing-research-spike/07-CONTEXT.md
@corpus/books.yaml
@.planning/research/PITFALLS.md

<interfaces>
<!-- Decision IDs from 07-CONTEXT.md that THIS plan implements: D-06, D-07, D-08, D-10, D-19 -->
<!-- This plan does NOT depend on Plan 02's recommendation in the dependency graph (both Wave 1), so we cannot assume
     which genre count won. Strategy: emit candidates for ALL 10 v1 genres in corpus_candidates.yaml regardless.
     If Plan 02 picks Proposal A (merge), Phase 8 selects from gothic + horror candidates as one bucket
     and scifi + fantasy as another. If Proposal B (drop horror + historical), Phase 8 simply ignores those keys.
     This avoids a hard sequence dependency between Plan 02 and Plan 03. -->
<!-- Current corpus author distribution (per 07-CONTEXT.md): 6 Austen romance, 6 Zane Grey western,
     5 Verne scifi, 4 Lovecraft horror, 4 Morris fantasy — the candidate shortlist must give each genre
     ≥8 distinct authors so Phase 8 has author-diversity headroom even after the final cut. -->
<!-- D-10 schema (verbatim from 07-CONTEXT.md):
     - {gutenberg_id: 84, title: "Frankenstein", author: "Mary Shelley",
        word_count: 75500, source: {provider: "gutenberg", fetched_at: "2026-06-XX", text_sha256: "abc..."}}
-->
<!-- D-05 reminder: zero installs. No gutenbergpy in Phase 7. Web-read Gutenberg's per-book pages and canon lists
     to populate the candidate list. The agent already has gutenbergpy installed but should use it READ-ONLY
     via existing cache if at all — no new downloads, no model retrains, no preprocessing reruns. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build per-genre candidate shortlists (corpus_candidates.yaml)</name>
  <files>.planning/research/v2/corpus_candidates.yaml</files>
  <read_first>
    - .planning/phases/07-corpus-sourcing-research-spike/07-CONTEXT.md (D-19 verbatim; the 50+ per-genre count)
    - corpus/books.yaml (current 100-book v1 corpus — every entry here is automatically a candidate; agent extends from this base)
    - .planning/research/v2/_drafts/02_source_evaluation.md if present (curation-source verdicts — drives which sources to actually use)
  </read_first>
  <action>
Create `.planning/research/v2/corpus_candidates.yaml` — a sibling YAML to the eventual `CORPUS_SOURCING.md`, structured for Phase 8 to consume.

Use this exact schema:

```yaml
# Per-genre candidate-title shortlist for v2 corpus expansion (Phase 7 output per D-19).
# Phase 8 selects the final 25 or 30 books per genre from these candidate lists.
# All candidates verified to exist on Project Gutenberg (text-fetch source per D-02).
# source_consensus_score = count of curation sources (Goodreads-UCSD shelves, LoC canon lists,
# scholarly canon lists, comparable-project corpora) on which this title appears.
# Higher score = more cross-source consensus that this title is canonical for its genre.

curation_sources_used:
  - name: "Goodreads-UCSD shelves"
    role: curation
    pattern: "Best-of-genre shelf membership; UCSD academic mirror"
  - name: "LoC canon lists"
    role: curation
    pattern: "Library of Congress curated genre lists (loc.gov/collections)"
  - name: "Scholarly canon lists"
    role: curation
    pattern: "Modern Library 100 Best Novels, Hugo/Nebula winners (scifi), MWA Edgar (mystery), Bram Stoker (horror), Western Writers of America Hall of Fame, Booker / Pulitzer (literary)"
  - name: "Comparable-project corpora"
    role: curation
    pattern: "Gutenberg Genre Identification (gjoseph16), Worsham & Kalita, BL Labs blbooksgenre title-level metadata"

genres:
  romance:
    target_count: 25-or-30  # Plan 02's recommendation drives the exact number; Phase 8 picks final N
    distinct_authors_required: 8  # D-08 hard constraint
    candidates:
      - {gutenberg_id: 1342, title: "Pride and Prejudice",       author: "Jane Austen",            source_consensus_score: 4}
      - {gutenberg_id: 161,  title: "Sense and Sensibility",     author: "Jane Austen",            source_consensus_score: 3}
      # ... continue to ≥50 entries
  mystery:
    target_count: 25-or-30
    distinct_authors_required: 8
    candidates:
      - {gutenberg_id: 2852, title: "The Hound of the Baskervilles", author: "Arthur Conan Doyle", source_consensus_score: 4}
      # ... ≥50 entries
  # ... all 10 v1 genres
```

For each of the 10 v1 genres (romance, mystery, western, fantasy, scifi, horror, historical, literary, adventure, gothic):

1. **Start with the 10 books already in `corpus/books.yaml`** — these are automatic candidates, give them `source_consensus_score: 4` (assume they passed v1 curation as effectively all-source-consensus).

2. **Extend each genre's list to ≥50 candidates** by sourcing from:
   - **Scholarly canon lists** for that genre (per the `curation_sources_used` block above):
     - Romance: Modern Library 100 Best Novels (romance entries), Library of Congress "Books That Shaped America" romance picks, Persuasion-era and Bronte-circle additions, Edith Wharton works, Henry James works, Gaskell, Trollope (Barsetshire), Hardy (Tess, Far from the Madding Crowd), early 20th-c. authors with PD works (D.H. Lawrence pre-1930, etc.)
     - Mystery: MWA Edgar Awards winners (older entries that are now PD), Doyle Sherlock collection (`A Scandal in Bohemia`, `The Sign of the Four`, etc. — many beyond `Hound`), Christie pre-1930 (`Poirot Investigates`, etc.), Sayers full Wimsey series pre-1928, Chesterton (Father Brown stories), Wallace Edgar, Carolyn Wells, Mary Roberts Rinehart, Anna Katharine Green
     - Western: WWoA Hall of Fame entries with PD works, additional Zane Grey (his catalog is huge), Owen Wister, Dane Coolidge, Andy Adams, B.M. Bower, Eugene Manlove Rhodes, Stewart Edward White, Frank H. Spearman, Bret Harte
     - Fantasy: Pre-Tolkien fantasy canon — Morris (multiple), Dunsany (multiple), Eddison, James Branch Cabell, George MacDonald (Phantastes, Lilith), C.S. Lewis pre-1930 (limited), William Hope Hodgson, Lord Dunsany short story collections, E. Nesbit, Howard Pyle, Andrew Lang fairy books
     - Scifi: Wells full catalog (12+ novels), Verne full catalog (15+ novels), Conan Doyle Professor Challenger series, Burroughs Mars + Pellucidar series, Olaf Stapledon (PD?), early scifi like Mary Shelley Frankenstein (also fits gothic), Garrett P. Serviss, Edward Bellamy, Stanley G. Weinbaum (if PD), early scifi short story collections
     - Horror: Lovecraft full pre-1930 catalog (his works are now mostly PD due to non-renewal), Poe full Tales, Le Fanu (multiple), Machen (multiple), James M.R. (Ghost Stories sequels), Blackwood (multiple), Bierce, Hodgson, Chambers (King in Yellow), Crawford, Stoker beyond Dracula
     - Historical: Dumas full Musketeers trilogy + Monte Cristo + others, Scott full Waverley novels, Tolstoy War & Peace + Anna Karenina (if Anna fits), Hugo Les Mis + Notre-Dame + Toilers of the Sea, Sienkiewicz, Dickens historicals, Cooper Leatherstocking, Wallace Ben-Hur, Hawthorne, Eliot historicals (Romola, Daniel Deronda)
     - Literary: Fitzgerald (PD-cleared 2021+), Hemingway (PD-cleared 2022+), Wharton, Woolf, Conrad, Lawrence, Joyce Dubliners (PD), Henry James, Sinclair Lewis (PD-cleared 2020+), Anderson Winesburg Ohio, Cather, Forster, Mansfield short stories
     - Adventure: Stevenson full (Treasure Island, Kidnapped, Catriona, Master of Ballantrae), London full (Call of the Wild, White Fang, Sea-Wolf, Martin Eden), Burroughs Tarzan series, Haggard full (She, Allan Quatermain, King Solomon's Mines, Cleopatra), Buchan (39 Steps, Greenmantle, Mr Standfast), Conrad (Lord Jim, Typhoon), Twain (Huck Finn, Tom Sawyer, Connecticut Yankee), Defoe (Robinson Crusoe, Moll Flanders)
     - Gothic: Radcliffe full (Udolpho, The Italian, A Sicilian Romance), Lewis Monk, Walpole Otranto, Maturin Melmoth the Wanderer, Hogg Confessions, Beckford Vathek, Mary Shelley (Frankenstein + The Last Man), Poe Gothic tales (House of Usher), Hawthorne Gothic tales, Le Fanu (also horror — list in both), Hogg, Brockden Brown (Wieland + Edgar Huntly + Arthur Mervyn), Charlotte Bronte Villette
   - **Goodreads-UCSD shelves** (where the agent can reach the public dump or has it cached): "best-classic-romance", "best-classic-mystery", "best-classic-horror", etc. — pick titles that appear on shelf AND have a Gutenberg id
   - **Comparable-project corpora** (from `_drafts/01_comparable_projects.md` Plan 01 Task 1): if the Gutenberg Genre Identification or Worsham & Kalita corpora are accessible at title level, cross-reference their genre tags
   - **Cross-check Gutenberg availability**: every candidate MUST have a working gutenberg_id. Look up via gutenberg.org/ebooks/{id} or use the local `gutenbergpy` cache (read-only). If a candidate is canonical but lacks a Gutenberg edition, exclude it (Standard Ebooks could be a future fallback per Plan 01 Task 2).

3. **Compute source_consensus_score** as integer count of curation sources on which the title appears. Range: 1 (one source agrees) to 4 (all 4 curation-source categories agree). Use your judgement; this is a coarse ranking signal, not a precision metric.

4. **Each genre's `candidates` list must have ≥50 entries** for the candidate count check to pass (D-19). If a genre has fewer than 50 strong candidates (likely for some niche-merged buckets), include weaker candidates with `source_consensus_score: 1` rather than padding with garbage. Document the limitation in a `notes:` field on that genre.

5. **Do NOT artificially cap per-author counts** (D-07). If Zane Grey has 25 Western works on Gutenberg and they're all in the candidate pool, list them. The author-diversity constraint (D-08) only requires ≥8 *distinct* authors total in the list, not a per-author limit.

6. **Sort each genre's candidates by source_consensus_score descending**. Ties broken by ascending gutenberg_id (deterministic ordering for Phase 8 reproducibility).

Length: this file will be ≥550 lines (10 genres × ~55 lines of candidates + headers). That's normal.

D-05: no installs. The agent reads Gutenberg's per-book pages via web fetch (if needed) or relies on training-data knowledge of public-domain canon. The `gutenbergpy` library is already installed (per CONTEXT.md `code_context`) — use it READ-ONLY if needed to verify a gutenberg_id resolves, but do not re-download or re-cache.
  </action>
  <verify>
    <automated>powershell -Command "$f='.planning/research/v2/corpus_candidates.yaml'; if(-not (Test-Path $f)){ Write-Host 'file missing'; exit 1 }; python -c ('import yaml,sys'+[char]10+'d=yaml.safe_load(open(r'+[char]39+$f+[char]39+',encoding='+[char]39+'utf-8'+[char]39+'))'+[char]10+'genres=d[chr(39)+chr(103)+chr(101)+chr(110)+chr(114)+chr(101)+chr(115)+chr(39)]'+[char]10+'expected=set([chr(39)+x+chr(39) for x in [chr(114)+chr(111)+chr(109)+chr(97)+chr(110)+chr(99)+chr(101)]])') 2>&1 | Out-Null; $expected=@('romance','mystery','western','fantasy','scifi','horror','historical','literary','adventure','gothic'); $py=@'+[char]10+'import yaml,sys'+[char]10+'d=yaml.safe_load(open(sys.argv[1],encoding=chr(39)+chr(117)+chr(116)+chr(102)+chr(45)+chr(56)+chr(39)))'+[char]10+'g=d[chr(39)+chr(103)+chr(101)+chr(110)+chr(114)+chr(101)+chr(115)+chr(39)]'+[char]10+'for k in [chr(39)+x+chr(39) for x in sys.argv[2:]]:'+[char]10+'    if k not in g: print(f[chr(39)]MISSING:{k}[chr(39)]); sys.exit(1)'+[char]10+'    c=g[k][chr(39)+chr(99)+chr(97)+chr(110)+chr(100)+chr(105)+chr(100)+chr(97)+chr(116)+chr(101)+chr(115)+chr(39)]'+[char]10+'    if len(c)<50: print(f[chr(39)]TOO_FEW:{k}={len(c)}[chr(39)]); sys.exit(1)'+[char]10+'    authors=set(e[chr(39)+chr(97)+chr(117)+chr(116)+chr(104)+chr(111)+chr(114)+chr(39)] for e in c)'+[char]10+'    if len(authors)<8: print(f[chr(39)]FEW_AUTHORS:{k}={len(authors)}[chr(39)]); sys.exit(1)'+[char]10+'print([chr(39)]OK[chr(39)])'+[char]10+'@'; $py | python - $f @expected"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/v2/corpus_candidates.yaml` exists
    - File parses as valid YAML (`python -c "import yaml; yaml.safe_load(open('...'))"` exits 0)
    - Top-level `genres:` key exists containing all 10 v1 genres: `romance`, `mystery`, `western`, `fantasy`, `scifi`, `horror`, `historical`, `literary`, `adventure`, `gothic`
    - Each genre's `candidates:` list has ≥ 50 entries
    - Each genre's candidates contain ≥ 8 distinct authors (set of `author` field values)
    - Every candidate entry has the four required keys: `gutenberg_id` (integer), `title` (string), `author` (string), `source_consensus_score` (integer 1-4)
    - File line count ≥ 550
    - `gutenberg_id` values are positive integers
  </acceptance_criteria>
  <done>Per-genre candidate YAML exists with ≥50 candidates per genre, ≥8 distinct authors per genre, sorted by source_consensus_score, ready for Phase 8 to draw 25 or 30 final picks from.</done>
</task>

<task type="auto">
  <name>Task 2: Draft author distribution audit + books.yaml schema fragment</name>
  <files>.planning/research/v2/_drafts/05_author_distribution_audit.md</files>
  <read_first>
    - .planning/phases/07-corpus-sourcing-research-spike/07-CONTEXT.md (D-06, D-07, D-08, D-10 verbatim)
    - .planning/research/v2/corpus_candidates.yaml (your Task 1 output — counts derived here)
    - corpus/books.yaml (v1 baseline for "before" numbers)
    - .planning/research/PITFALLS.md §5 (author overlap leakage — the audit IS the surface-level mitigation)
  </read_first>
  <action>
Create `.planning/research/v2/_drafts/05_author_distribution_audit.md`. Skeleton:

```markdown
# Corpus shape — author distribution audit (Phase 7 draft fragment — to be assembled into CORPUS_SOURCING.md)

> Sources for CORPUS_SOURCING.md §"Author distribution audit" and §"books.yaml schema additions". Decision IDs implemented: D-06, D-07, D-08, D-10.

## Author distribution audit

Per D-08 (hard constraint): every genre must have ≥8 distinct authors. Per D-07: no per-author cap inside a genre. The combination is the controlled trade: we accept prolific-author concentration in exchange for a tight per-author held-out smoke test (D-17, ≤10pp gap pass criterion, defined in VALIDATION_PROTOCOL.md).

### v1 baseline (current 100-book corpus)

The current `corpus/books.yaml` shows severe author concentration:

| Genre | Distinct authors / 10 | Most-prolific author / count | Meets D-08 floor (≥8)? |
|-------|----------------------|------------------------------|------------------------|
| romance | <N> | <Austen / 6> | <yes/no — no, 4 distinct> |
| mystery | <N> | <Christie / 3 or Doyle / 2> | <yes/no> |
| western | <N> | <Zane Grey / 6> | <yes/no — no, 4 distinct> |
| fantasy | <N> | <Morris / 4> | <yes/no> |
| scifi | <N> | <Verne / 5> | <yes/no> |
| horror | <N> | <Lovecraft / 4> | <yes/no> |
| historical | <N> | <Dumas / 3> | <yes/no> |
| literary | <N> | <Hemingway / 2 + Wharton / 2 + Woolf / 2> | <yes/no> |
| adventure | <N> | <Stevenson / 2 + London / 2> | <yes/no> |
| gothic | <N> | <variable> | <yes/no> |

(Compute the actual N values from `corpus/books.yaml`. Use Python or hand-count.)

**Finding:** several v1 genres violate D-08. The candidate-shortlist in `corpus_candidates.yaml` corrects this — every genre's candidate list has ≥8 distinct authors per Task 1's verification.

### v2 candidate-list audit (from `corpus_candidates.yaml`)

| Genre | Distinct authors in candidate list | Most-prolific candidate author / count | D-08 satisfied (≥8 distinct)? |
|-------|-----------------------------------|----------------------------------------|-------------------------------|
| romance | <N>  | <author / count> | yes |
| mystery | <N>  | <author / count> | yes |
| western | <N>  | <author / count> | yes |
| fantasy | <N>  | <author / count> | yes |
| scifi | <N>  | <author / count> | yes |
| horror | <N>  | <author / count> | yes |
| historical | <N>  | <author / count> | yes |
| literary | <N>  | <author / count> | yes |
| adventure | <N>  | <author / count> | yes |
| gothic | <N>  | <author / count> | yes |

(Run a small Python snippet — inline below — against your `corpus_candidates.yaml` to compute these. Do NOT install anything; PyYAML is already installed for v1.)

```python
# Reference snippet — agent runs this and pastes the output into the table above.
import yaml
from collections import Counter
data = yaml.safe_load(open('.planning/research/v2/corpus_candidates.yaml', encoding='utf-8'))
for genre, g in data['genres'].items():
    authors = Counter(c['author'] for c in g['candidates'])
    distinct = len(authors)
    most_prolific = authors.most_common(1)[0] if authors else ('-', 0)
    print(f"{genre}: {distinct} distinct authors; most-prolific = {most_prolific[0]} × {most_prolific[1]}")
```

### Per-author held-out smoke test prerequisite (D-17 cross-reference)

VALIDATION_PROTOCOL.md (Plan 07 output) specifies a per-author held-out smoke test with ≤10pp gap pass criterion. That test depends on this audit:

- The set of authors with ≥2 books in the final corpus (Phase 8 picks) drives the test set.
- Given D-07 (no per-author cap), the prolific-author rows in this audit identify the highest-risk authors. The smoke test will hold each of them out completely and verify the model still classifies their works correctly.
- Authors with only 1 book in the final corpus are not part of the smoke test (no within-author signal possible).

### Corpus shape decisions (D-06, D-07, D-08)

- **Books per genre (D-06):** <25 if Plan 02 picked Proposal C / 30 if Plan 02 picked Proposal A or B>. Single number, no range.
- **Genre count:** <10 if Proposal C / 8 if Proposal A or B>
- **Total corpus size:** <250 or 240>
- **Per-author cap (D-07):** **NONE**. Prolific public-domain authors who define a genre (Austen, Wells, Verne, Zane Grey, Lovecraft, Dumas) may contribute as many works as the candidate shortlist + Phase 8's final selection process retains. The risk is documented; the mitigation is D-17 not a per-author cap.
- **Distinct authors per genre (D-08):** ≥8 distinct authors per genre is a hard constraint. Candidate shortlists already satisfy this; Phase 8 must preserve it when picking the final 25 or 30.

### Phase 8 selection rule

Given a genre's candidate list (≥50 entries, ≥8 distinct authors, sorted by source_consensus_score desc), Phase 8's selection procedure is:

1. Take the top-scored title from each distinct author (guarantees ≥8 distinct authors covered).
2. Fill remaining slots (25-8=17, or 30-8=22) by source_consensus_score descending, breaking ties by ascending gutenberg_id.
3. Skip any candidate that fails the word-count minimum (Phase 8 picks a threshold; tentatively ≥20,000 words per book — short story collections may have lower individual entries but full collections clear the bar).
4. Skip any candidate whose Gutenberg edition is missing or corrupt (Phase 8 catches this at fetch time).
5. Record the dropped/replaced candidates in Phase 8's corpus-build log.

This procedure is deterministic and reproducible — Phase 8's `scripts/build_corpus.py` (CEXP-05 P2) can emit the same final list from the same `corpus_candidates.yaml`.

## books.yaml schema additions (D-10)

Phase 7 specifies these additions; Phase 8 applies them to `corpus/books.yaml` during CEXP-01.

**Existing schema (v1):**

```yaml
- {gutenberg_id: 84, title: "Frankenstein", author: "Mary Shelley", word_count: 75500}
```

**v2 additions:**

```yaml
- {gutenberg_id: 84, title: "Frankenstein", author: "Mary Shelley",
   word_count: 75500,
   source:
     provider: "gutenberg"           # one of: gutenberg, standard_ebooks, internet_archive
     fetched_at: "2026-06-XX"        # ISO date of the text fetch
     text_sha256: "abc123def456..."} # sha256 of the canonical preprocessed text bytes
```

**Field semantics:**

- `source.provider` — string enum, lowercase snake_case. Identifies which text-fetch source per the verdicts in `02_source_evaluation.md`. v2 expects mostly `"gutenberg"`; `"standard_ebooks"` permitted for titles where SE has a cleaner edition; `"internet_archive"` only as the documented escape hatch.
- `source.fetched_at` — ISO 8601 date (YYYY-MM-DD). Records when the text bytes were retrieved. Phase 8's `scripts/build_corpus.py` populates this at fetch time.
- `source.text_sha256` — hex string (64 chars). sha256 of the post-preprocessing canonical bytes (after Gutenberg header/footer stripping, before tokenisation). Used together with `corpus_hash` (BUG-05 in Phase 6) to detect silent corpus drift.

**Backward compatibility:** the new `source` field is additive. `backend/api/routes/corpus.py::_load_books_metadata()` ignores unknown keys — verify no schema validator rejects extra fields. Phase 8 verifies this with a fixture loaded against the new schema before bulk-applying.

**Single source of truth:** `corpus/books.yaml` is the only manifest. There is NO sibling `corpus/sources.yaml`. All provenance lives on each book.

## Phase 7 self-check

Per D-06: ☑ single number committed (not a range)
Per D-07: ☑ no per-author cap imposed in candidate list
Per D-08: ☑ ≥8 distinct authors per genre in candidate list
Per D-10: ☑ schema spec with provider/fetched_at/text_sha256 written above; Phase 8 applies it
Per D-19: ☑ candidate list ≥50 per genre (see `corpus_candidates.yaml`)
```

Replace every `<...>` with real content. Run the reference Python snippet to fill in actual counts. Both the v1 audit table and the v2 candidate-list audit table must be filled out (not left as `<N>`).

D-05: no installs. PyYAML is already installed; the inline snippet uses only stdlib + yaml.

Length: 80-200 lines.
  </action>
  <verify>
    <automated>powershell -Command "$f='.planning/research/v2/_drafts/05_author_distribution_audit.md'; $main=(Select-String -Path $f -Pattern '## Author distribution audit' -Quiet); $v1=(Select-String -Path $f -Pattern '### v1 baseline' -Quiet); $v2=(Select-String -Path $f -Pattern '### v2 candidate-list audit' -Quiet); $sm=(Select-String -Path $f -Pattern '### Per-author held-out smoke test prerequisite' -Quiet); $shape=(Select-String -Path $f -Pattern '### Corpus shape decisions' -Quiet); $schema=(Select-String -Path $f -Pattern '## books\.yaml schema additions' -Quiet); $d06=(Select-String -Path $f -Pattern 'Books per genre.*(25|30)' -Quiet); $d07=(Select-String -Path $f -Pattern 'Per-author cap.*NONE' -Quiet); $d08=(Select-String -Path $f -Pattern '≥8 distinct authors' -Quiet); $provider=(Select-String -Path $f -Pattern 'source\.provider' -Quiet); $fetched=(Select-String -Path $f -Pattern 'source\.fetched_at' -Quiet); $sha=(Select-String -Path $f -Pattern 'source\.text_sha256' -Quiet); if($main -and $v1 -and $v2 -and $sm -and $shape -and $schema -and $d06 -and $d07 -and $d08 -and $provider -and $fetched -and $sha){ exit 0 } else { Write-Host ('main:'+$main+' v1:'+$v1+' v2:'+$v2+' sm:'+$sm+' shape:'+$shape+' schema:'+$schema+' d06:'+$d06+' d07:'+$d07+' d08:'+$d08+' fields:'+$provider+'/'+$fetched+'/'+$sha); exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/v2/_drafts/05_author_distribution_audit.md` exists
    - File contains heading `## Author distribution audit` (exact text)
    - File contains all subsection headings: `### v1 baseline`, `### v2 candidate-list audit`, `### Per-author held-out smoke test prerequisite`, `### Corpus shape decisions`
    - File contains heading `## books.yaml schema additions` (exact text)
    - File contains line `**Books per genre (D-06):**` followed by either `25` or `30`
    - File contains line containing `Per-author cap (D-07):` AND the literal string `NONE`
    - File contains the literal string `≥8 distinct authors` (D-08 reference)
    - File contains the literal strings `source.provider`, `source.fetched_at`, `source.text_sha256` (the three D-10 schema fields)
    - File line count ≥ 80
    - v1 baseline table values are not left as `<N>` placeholders (verify with `grep -c "<N>"` returning 0 — no remaining angle-bracket placeholders)
  </acceptance_criteria>
  <done>Author distribution audit exists with real v1 and v2 numbers, D-06/07/08 commitments, D-10 schema specification, and a Phase 8 selection rule that's deterministic given the candidate YAML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web→agent | Web reads of Gutenberg metadata and canon list pages |
| local→file | YAML write to `.planning/research/v2/corpus_candidates.yaml` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | I (Information disclosure) | Web reads of Gutenberg / canon lists | accept | All sources public; no credentials. |
| T-07-07 | T (Tampering) | Candidate YAML schema | mitigate | Schema is documented; Plan 05 + Phase 8 both validate against the schema (gutenberg_id integer, title/author strings, source_consensus_score integer 1-4). |
| T-07-08 | I (Information disclosure) | Author lists | accept | All author names are public-domain authors; no PII. |
</threat_model>

<verification>
After both tasks: one YAML + one markdown fragment exist under `.planning/research/v2/`. YAML parses, contains all 10 genres with ≥50 candidates each and ≥8 distinct authors each. Markdown contains all required headings and D-XX numeric commitments.
</verification>

<success_criteria>
- D-19: ≥50 gutenberg_ids per genre in candidate YAML
- D-08: ≥8 distinct authors per genre in candidate YAML (hard constraint)
- D-07: no per-author cap applied to candidates
- D-06: single per-genre book count number committed (25 or 30) in audit fragment
- D-10: books.yaml schema additions documented with provider/fetched_at/text_sha256
- All 10 v1 genres represented; Plan 02's recommendation determines which are used by Phase 8 (merge/drop/keep)
</success_criteria>

<output>
After completion, create `.planning/phases/07-corpus-sourcing-research-spike/07-03-SUMMARY.md` listing the candidate YAML path, per-genre author counts (verifying D-08), and the schema additions for Phase 8 to apply.
</output>