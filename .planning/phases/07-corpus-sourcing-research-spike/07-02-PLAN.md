---
phase: 07-corpus-sourcing-research-spike
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v2/_drafts/04_genre_set_recommendation.md
  - .planning/research/v2/_drafts/04a_lcc_subject_overlap.md
autonomous: true
requirements: [RES-01]
tags: [research, corpus, genres]

must_haves:
  truths:
    - "Genre-set recommendation fragment exists with a single explicit Proposal A / B / C recommendation"
    - "All three proposals (A merge, B drop, C keep) are documented with pros/cons and evidence — recommendation is NOT free-choice, it must pick one of the three"
    - "LCC subject overlap appendix exists with a pairwise overlap matrix or table for the 10 v1 genres"
    - "Recommendation cites at least 3 evidence categories: LCC subject overlap, comparable-project precedent (referencing Plan 01 Task 1 output), per-genre public-domain author availability"
    - "Final genre count is committed to a single number (either 8 or 10) consistent with the chosen proposal — no ranges, no maybes"
    - "Recommendation explicitly states whether the count drives D-06 to 25 books/genre (10 genres) or 30 books/genre (8 genres)"
    - "Merged-genre label names use v1 snake-case convention if Proposal A wins (e.g., `gothic_horror`, `speculative` — matching v1's `scifi`)"
  artifacts:
    - path: ".planning/research/v2/_drafts/04_genre_set_recommendation.md"
      provides: "Genre-set proposal evaluation and single recommendation per D-09 + D-21"
      contains: "## Genre set recommendation"
      min_lines: 80
    - path: ".planning/research/v2/_drafts/04a_lcc_subject_overlap.md"
      provides: "LCC subject overlap analysis appendix supporting the genre-set recommendation"
      contains: "## LCC subject overlap"
      min_lines: 30
  key_links:
    - from: ".planning/research/v2/_drafts/04_genre_set_recommendation.md"
      to: ".planning/research/v2/_drafts/04a_lcc_subject_overlap.md"
      via: "Recommendation cites the overlap appendix as evidence"
      pattern: "04a_lcc_subject_overlap|LCC subject overlap"
    - from: ".planning/research/v2/_drafts/04_genre_set_recommendation.md"
      to: ".planning/research/v2/_drafts/01_comparable_projects.md"
      via: "Recommendation cites comparable-project genre choices as evidence"
      pattern: "comparable|Plan 01|01_comparable"
---

<objective>
Produce the genre-set recommendation fragment for `CORPUS_SOURCING.md §"Genre set recommendation"` plus a supporting LCC subject overlap appendix. This is the **highest-leverage decision in Phase 7** (per 07-CONTEXT.md `<specifics>`): which genres survive into v2 determines per-genre book count (D-06), final corpus size, and which weak v1 performers (horror 22%, scifi 40%, historical 30%) get re-sourced vs merged vs dropped.

Per D-09, three proposals are on the table:
- **Proposal A** — Merge to 8 genres: `gothic + horror → gothic_horror`, `scifi + fantasy → speculative`; keep adventure, historical, literary, mystery, romance, western
- **Proposal B** — Drop weak performers (8 genres): drop horror + historical; keep adventure, fantasy, gothic, literary, mystery, romance, scifi, western
- **Proposal C** — Keep all 10 v1 genres; bet that 25-30 books × ≥8 authors per genre surfaces real per-genre topology

Per D-21, the recommendation surfaces in CORPUS_SOURCING.md for inline user review during normal doc reading. No mid-phase checkpoint. The user reviews the assembled CORPUS_SOURCING.md at end-of-phase and approves or pushes back.

Purpose: Reach a defensible single recommendation backed by three evidence streams (LCC overlap, comparable-project precedent, public-domain author availability) so Plan 05 assembly slots this in unchanged.

Output: Two markdown fragments under `.planning/research/v2/_drafts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-corpus-sourcing-research-spike/07-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
@corpus/books.yaml
@results/validation_report.txt

<interfaces>
<!-- Decision IDs from 07-CONTEXT.md that THIS plan implements: D-09, D-21 -->
<!-- Current v1 corpus (corpus/books.yaml): 10 genres × 10 books = 100 books -->
<!-- Genres: romance, mystery, western, fantasy, scifi, horror, historical, literary, adventure, gothic -->
<!-- v1 per-genre LOOCV accuracy (results/validation_report.txt):
     western 88.9%, mystery 70.0%, romance 70.0%, fantasy 66.7%, gothic 60.0%, literary 60.0%,
     adventure 40.0%, scifi 40.0%, historical 30.0%, horror 22.2% -->
<!-- Severe author concentration (from 07-CONTEXT.md domain block):
     6 Austen / 10 romance, 6 Zane Grey / 10 western, 5 Verne / 10 scifi,
     4 Lovecraft / 10 horror, 4 Morris / 10 fantasy -->
<!-- D-09 constraint: research agent investigates LCC subject overlap, comparable-project precedent,
     per-genre public-domain author availability — then recommends Proposal A / B / C -->
<!-- D-21 constraint: recommendation surfaces in CORPUS_SOURCING.md for inline user review; no mid-phase checkpoint -->
<!-- Snake-case naming for merged genres (Claude's discretion per 07-CONTEXT.md): if Proposal A wins,
     use `gothic_horror` and `speculative` (or `scifi_fantasy`) — consistent with v1's `scifi` (no hyphens, no spaces) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LCC subject overlap appendix</name>
  <files>.planning/research/v2/_drafts/04a_lcc_subject_overlap.md</files>
  <read_first>
    - .planning/phases/07-corpus-sourcing-research-spike/07-CONTEXT.md (D-09 wording; investigates LCC subject overlap)
    - corpus/books.yaml (current 100 books with author + gutenberg_id; agent can look up LCC subjects via Gutenberg metadata for each)
    - .planning/research/PITFALLS.md §5 (author overlap leakage — context for why subject overlap matters)
  </read_first>
  <action>
Create `.planning/research/v2/_drafts/04a_lcc_subject_overlap.md`. This is an APPENDIX supporting the main recommendation; keep it focused, not exhaustive.

Use this skeleton:

```markdown
# LCC subject overlap analysis (Phase 7 draft fragment — appendix for CORPUS_SOURCING.md)

> Evidence supporting CORPUS_SOURCING.md §"Genre set recommendation". Referenced from `04_genre_set_recommendation.md`.

## LCC subject overlap

For each v1 genre, we identify the dominant LCC subject headings (via Project Gutenberg metadata) and report pairwise overlap between genres. Two genres "overlap" if a non-trivial fraction of their books share LCC subject headings.

### Methodology

- For each v1 genre, list the LCC subjects appearing on ≥3 books in that genre
- For each pair of genres, count books in genre A whose dominant LCC subject also appears in genre B's dominant-subject set
- High overlap → genres are competing for the same feature-space region → merge candidate
- Low overlap → genres are topologically separable → keep candidate

### Per-genre dominant LCC subjects

| Genre | v1 books | Dominant LCC subjects (top 3) |
|-------|----------|-------------------------------|
| romance | 10 | <e.g., "PR4034 — Austen", "PR4145 — Bronte sisters", "PR — English literature 19th century"> |
| mystery | 10 | <e.g., "PR — Detective and mystery stories"> |
| western | 10 | <e.g., "PS — Western stories — Fiction"> |
| fantasy | 10 | <e.g., "PR — Fantasy fiction"> |
| scifi | 10 | <e.g., "PR/PS — Science fiction"> |
| horror | 10 | <e.g., "PR — Horror tales", "PS — Lovecraft"> |
| historical | 10 | <e.g., "PR — Historical fiction", "PQ — French historical"> |
| literary | 10 | <e.g., "PR/PS — Literary fiction 20th century"> |
| adventure | 10 | <e.g., "PR/PS — Adventure stories"> |
| gothic | 10 | <e.g., "PR — Gothic novels"> |

(Fill in real LCC subject headings the agent finds in Gutenberg metadata. If a genre's books don't have consistent LCC subjects in Gutenberg, note this as a finding.)

### Pairwise overlap (10×10 matrix or sparse list)

<Use whichever format is more readable. Two options:

OPTION A — Compact table form (subset of pairs with overlap ≥ 30%):

| Genre A | Genre B | Shared books | Overlap % | Note |
|---------|---------|--------------|-----------|------|
| gothic | horror | <N> | <%> | <e.g., "Carmilla, Frankenstein, Dracula all carry both Gothic and Horror subject tags"> |
| scifi | fantasy | <N> | <%> | <e.g., "Burroughs (Tarzan + A Princess of Mars) tagged both"> |
| scifi | adventure | <N> | <%> | <e.g., "Verne novels tagged Adventure as well as Science fiction"> |
| historical | adventure | <N> | <%> | <e.g., "The Three Musketeers, Ivanhoe tagged both"> |
...

OPTION B — Full 10×10 matrix if the agent generates one cleanly.

Either format is acceptable.>

### Findings

- **High overlap (>30%):** <list the pairs — these are merge candidates>
- **Moderate overlap (15-30%):** <list — context for the merge-vs-keep decision>
- **Low overlap (<15%):** <list — these are clearly separable in LCC space>

### Implication for the genre-set decision

- **Proposal A (merge gothic+horror, scifi+fantasy):** Supported if gothic↔horror overlap >30% AND scifi↔fantasy overlap >30%
- **Proposal B (drop horror, historical):** Supported if horror has low overlap with everything but also poor v1 performance (22.2% — yes), AND historical has low overlap (30% v1)
- **Proposal C (keep 10):** Supported if all 10 genres have <30% pairwise overlap — implies feature space CAN separate them and v1's poor accuracy is a corpus-sparsity problem, not a separability problem

The recommendation in `04_genre_set_recommendation.md` cites this analysis as one of its three evidence streams.
```

Practical note for the agent: Gutenberg's LCC tagging is uneven. If LCC data is sparse or missing for many v1 books, document this as a finding: "LCC overlap analysis is limited by Gutenberg's inconsistent subject tagging — N of 100 books had no LCC subject; conclusions are best-effort. Proposal A/B/C recommendation accordingly weights the other two evidence streams more heavily."

Do NOT install new dependencies. Use Project Gutenberg's metadata via web reads (gutenberg.org's per-book pages) or the local `gutenbergpy` cache if it's already populated. If neither yields clean data, fall back to manually-curated knowledge: e.g., "Carmilla, Dracula, Frankenstein are widely catalogued under both Gothic and Horror" can be stated without a hard LCC citation as long as the limitation is noted.

Length: 30-100 lines.
  </action>
  <verify>
    <automated>powershell -Command "$f='.planning/research/v2/_drafts/04a_lcc_subject_overlap.md'; $h=(Select-String -Path $f -Pattern '## LCC subject overlap' -Quiet); $method=(Select-String -Path $f -Pattern '### Methodology' -Quiet); $perGenre=(Select-String -Path $f -Pattern '### Per-genre dominant LCC subjects' -Quiet); $pairwise=(Select-String -Path $f -Pattern '### Pairwise overlap' -Quiet); $findings=(Select-String -Path $f -Pattern '### Findings' -Quiet); $impl=(Select-String -Path $f -Pattern '### Implication for the genre-set decision' -Quiet); if($h -and $method -and $perGenre -and $pairwise -and $findings -and $impl){ exit 0 } else { Write-Host ('h:'+$h+' m:'+$method+' pg:'+$perGenre+' pw:'+$pairwise+' f:'+$findings+' i:'+$impl); exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/v2/_drafts/04a_lcc_subject_overlap.md` exists
    - File contains heading `## LCC subject overlap` (exact text)
    - File contains all five subsection headings: `### Methodology`, `### Per-genre dominant LCC subjects`, `### Pairwise overlap`, `### Findings`, `### Implication for the genre-set decision`
    - File contains the per-genre table (markdown `| Genre |` table header) covering all 10 v1 genres: romance, mystery, western, fantasy, scifi, horror, historical, literary, adventure, gothic
    - File line count ≥ 30
    - No installs performed
  </acceptance_criteria>
  <done>LCC subject overlap appendix exists with per-genre dominant subjects, pairwise overlap data, and explicit implication for each of Proposals A / B / C.</done>
</task>

<task type="auto">
  <name>Task 2: Draft genre-set recommendation fragment</name>
  <files>.planning/research/v2/_drafts/04_genre_set_recommendation.md</files>
  <read_first>
    - .planning/phases/07-corpus-sourcing-research-spike/07-CONTEXT.md (D-09 verbatim; the three proposals)
    - .planning/research/v2/_drafts/04a_lcc_subject_overlap.md (your Task 1 output — recommendation cites it)
    - corpus/books.yaml (per-genre author availability evidence)
    - results/validation_report.txt (per-genre v1 LOOCV accuracy — context for "weak performer" claims)
    - .planning/research/v2/_drafts/01_comparable_projects.md if present at write time (sibling from Plan 01 Task 1 — comparable-project genre choices as evidence)
  </read_first>
  <action>
Create `.planning/research/v2/_drafts/04_genre_set_recommendation.md`. Use this exact skeleton:

```markdown
# Genre set recommendation (Phase 7 draft fragment — to be assembled into CORPUS_SOURCING.md)

> Sources for CORPUS_SOURCING.md §"Genre set recommendation". Decision IDs implemented: D-09, D-21. User reviews this recommendation inline during normal doc review — no separate checkpoint.

## Genre set recommendation

v1 ships with 10 genres at 10 books each (100 total). Per-genre v1 LOOCV accuracy (from `results/validation_report.txt`):

| Genre | v1 LOOCV accuracy |
|-------|-------------------|
| western | 88.9% |
| mystery | 70.0% |
| romance | 70.0% |
| fantasy | 66.7% |
| gothic | 60.0% |
| literary | 60.0% |
| adventure | 40.0% |
| scifi | 40.0% |
| historical | 30.0% |
| horror | 22.2% |

The three weakest performers (horror, historical, scifi/adventure) drive the recommendation. Three proposals are on the table per D-09:

### Proposal A — Merge to 8 genres
Merge `gothic + horror → gothic_horror`; merge `scifi + fantasy → speculative` (snake_case label consistent with v1's `scifi`). Keep: adventure, historical, literary, mystery, romance, western.

- **Pros:** LCC subject overlap analysis shows <%-figure>% gothic↔horror overlap and <%-figure>% scifi↔fantasy overlap (see `04a_lcc_subject_overlap.md`). The weak horror performer (22%) likely collapses into gothic's stronger feature-space region; the moderate scifi (40%) and fantasy (67%) genres are stylistically adjacent (both Burroughs, both Wells-style speculative). 8 genres × 30 books = 240-book corpus — larger per-genre sample at the same total budget.
- **Cons:** Loses the analytical ability to distinguish gothic from horror as separate categories; some users want to upload "modern horror" and see it not classified as gothic. Speculative-as-megagenre obscures sub-stylistic difference between scifi and fantasy.
- **What D-06 becomes:** 30 books × 8 genres = 240 books.

### Proposal B — Drop weak performers (8 genres)
Drop horror + historical. Keep: adventure, fantasy, gothic, literary, mystery, romance, scifi, western.

- **Pros:** Accepts the empirical finding that horror (22%) and historical (30%) can't be separated in feature space — rather than try to fix sourcing, accept the limitation. Removes the worst contributors to overall accuracy. Each surviving genre is a confident classification target.
- **Cons:** Loses two genres entirely; users who upload horror or historical fiction get classified into whichever of the 8 remaining genres is feature-space-closest, with no honest "this doesn't fit any of our trained genres" signal. Public-domain horror catalog is rich (Lovecraft, Poe, Stoker, Le Fanu, Machen, James, Blackwood, Hodgson, Bierce, Chambers...) and historical is even richer (Dumas, Scott, Hugo, Tolstoy, Dickens, Hawthorne, Wallace, Stevenson, Sienkiewicz...) — dropping them ignores plenty of available material.
- **What D-06 becomes:** 30 books × 8 genres = 240 books.

### Proposal C — Keep all 10 v1 genres
Bet that 25-30 books × ≥8 authors per genre surfaces real per-genre topology that the current 100-book corpus is too sparse and too author-overfitted to show.

- **Pros:** Per-genre author concentration is severe in v1 (6 Austen/10 romance, 6 Zane Grey/10 western, 5 Verne/10 scifi, 4 Lovecraft/10 horror, 4 Morris/10 fantasy — per 07-CONTEXT.md). Tripling books-per-genre with ≥8 distinct authors per D-08 may surface real signal that's currently swamped by author style. Keeps the full v1 surface intact; no genre removed from the product story; honest "we tried to separate these and here's how well we did" framing.
- **Cons:** Highest sourcing effort (250 books vs 240 for A/B). Horror and historical's poor v1 performance may persist — feature-space separability may be a real limit, not a corpus-size limit. Risk: ship v2 with same 22% horror accuracy after 2.5x sourcing investment.
- **What D-06 becomes:** 25 books × 10 genres = 250 books.

## Evidence

### Evidence 1: LCC subject overlap

See `04a_lcc_subject_overlap.md` (this fragment's sibling appendix). Summary:

- gothic ↔ horror overlap: <%-figure>%
- scifi ↔ fantasy overlap: <%-figure>%
- horror has <low|moderate|high> overlap with every other genre except gothic
- historical has <low|moderate|high> overlap with adventure and literary
- All other pairs <low|moderate>% overlap

This evidence <strongly supports | weakly supports | argues against> <Proposal A | Proposal B | Proposal C>.

### Evidence 2: Comparable-project precedent

Cite `01_comparable_projects.md` (sibling fragment from Plan 01 Task 1) — what genre count did 3-5 comparable projects use?

- <Project 1>: <N genres, names>
- <Project 2>: <N genres, names>
- <Project 3>: <N genres, names>

Cross-project consensus on genre count for book-length literary text: <typically N to N>. Cross-project consensus on merged categories: <e.g., "BL Labs uses fiction/nonfiction binary — not comparable; Gutenberg Genre Identification uses 10 distinct genres; Worsham & Kalita uses 10 with no merges">.

This evidence <strongly supports | weakly supports | argues against> <Proposal A | Proposal B | Proposal C>.

### Evidence 3: Per-genre public-domain author availability

For each genre, count distinct public-domain authors known to have ≥2 works in that genre on Project Gutenberg. ≥8 distinct authors (per D-08) is the hard constraint.

| Genre | Distinct PD authors with ≥2 works (estimated) | Meets D-08 floor (≥8)? |
|-------|------------------------------------------------|------------------------|
| romance | <N> | <yes/no> |
| mystery | <N> | <yes/no> |
| western | <N> | <yes/no> |
| fantasy | <N> | <yes/no> |
| scifi | <N> | <yes/no> |
| horror | <N> | <yes/no> |
| historical | <N> | <yes/no> |
| literary | <N> | <yes/no> |
| adventure | <N> | <yes/no> |
| gothic | <N> | <yes/no> |

For Proposal A merge genres:
- gothic_horror combined: <N distinct PD authors with ≥2 works in either gothic OR horror>
- speculative combined (scifi + fantasy): <N distinct PD authors>

This evidence <strongly supports | weakly supports | argues against> <Proposal A | Proposal B | Proposal C>.

## Recommendation

**Recommendation: Proposal <A | B | C>.**

**Rationale:** <2-3 sentences synthesizing the three evidence streams. Example template — agent picks one based on the actual evidence found:
 - Proposal A wins if: LCC overlap is high for the two merge pairs AND comparable projects show precedent for merging AND merge-author-counts comfortably exceed ≥8.
 - Proposal B wins if: horror + historical author availability is below or near the ≥8 floor AND LCC analysis shows they're not separable AND comparable projects routinely drop unstable categories.
 - Proposal C wins if: all 10 genres have ≥8 PD authors with ≥2 works AND LCC overlap is <30% for all pairs AND comparable projects favour preserving genre count over merging.>

**Final genre count:** <8 | 10>.
**Final books per genre (D-06):** <30 if 8 | 25 if 10>.
**Final total corpus size:** <240 | 250> books.

**Snake-case labels (if Proposal A wins):** `gothic_horror`, `speculative` (or `scifi_fantasy` — agent picks one; must be consistent with v1's `scifi` naming).

**User checkpoint:** Per D-21, this recommendation surfaces in `CORPUS_SOURCING.md §"Genre set recommendation"` for inline user review during normal doc review. The user reads the assembled document and either approves the recommendation (implicit by proceeding to Phase 8) or pushes back via follow-up `/gsd-fast` or comments.
```

Replace every `<...>` placeholder with real content. The recommendation MUST pick one of A, B, or C — not "lean toward A pending more data". The committed number (8 or 10) drives D-06 (30 books/genre or 25 books/genre).

Length: 80-200 lines.
  </action>
  <verify>
    <automated>powershell -Command "$f='.planning/research/v2/_drafts/04_genre_set_recommendation.md'; $main=(Select-String -Path $f -Pattern '## Genre set recommendation' -Quiet); $pA=(Select-String -Path $f -Pattern '### Proposal A' -Quiet); $pB=(Select-String -Path $f -Pattern '### Proposal B' -Quiet); $pC=(Select-String -Path $f -Pattern '### Proposal C' -Quiet); $ev1=(Select-String -Path $f -Pattern '### Evidence 1: LCC subject overlap' -Quiet); $ev2=(Select-String -Path $f -Pattern '### Evidence 2: Comparable-project precedent' -Quiet); $ev3=(Select-String -Path $f -Pattern '### Evidence 3: Per-genre public-domain author availability' -Quiet); $rec=(Select-String -Path $f -Pattern '\*\*Recommendation: Proposal (A|B|C)\.\*\*' -Quiet); $count=(Select-String -Path $f -Pattern '\*\*Final genre count:\*\* (8|10)' -Quiet); $books=(Select-String -Path $f -Pattern '\*\*Final books per genre.*:\*\* (25|30)' -Quiet); if($main -and $pA -and $pB -and $pC -and $ev1 -and $ev2 -and $ev3 -and $rec -and $count -and $books){ exit 0 } else { Write-Host ('main:'+$main+' A:'+$pA+' B:'+$pB+' C:'+$pC+' e1:'+$ev1+' e2:'+$ev2+' e3:'+$ev3+' rec:'+$rec+' count:'+$count+' books:'+$books); exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/v2/_drafts/04_genre_set_recommendation.md` exists
    - File contains heading `## Genre set recommendation` (exact text)
    - File contains all three proposal subsections: `### Proposal A`, `### Proposal B`, `### Proposal C` (exact prefixes)
    - File contains all three evidence subsections: `### Evidence 1: LCC subject overlap`, `### Evidence 2: Comparable-project precedent`, `### Evidence 3: Per-genre public-domain author availability` (exact text)
    - File contains a line matching the pattern `**Recommendation: Proposal A.**` or `**Recommendation: Proposal B.**` or `**Recommendation: Proposal C.**` (exactly one of these)
    - File contains a line matching `**Final genre count:** 8` or `**Final genre count:** 10`
    - File contains a line matching `**Final books per genre` followed somewhere by `25` or `30`
    - File contains a reference to `04a_lcc_subject_overlap.md` (or `LCC subject overlap` from the appendix's title) confirming the evidence link
    - File line count ≥ 80
  </acceptance_criteria>
  <done>Genre-set recommendation fragment exists with three proposals fully evaluated, three evidence streams documented, and a single explicit recommendation that determines D-06's number and snake-case labels if merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web→agent | Research agent reads Gutenberg / LCC web pages for subject-tag data — untrusted content crosses into local markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | I (Information disclosure) | Gutenberg + LoC public catalog reads | accept | All sources public; no credentials. |
| T-07-05 | T (Tampering) | LCC subject data quality from Gutenberg metadata | mitigate | Document limitation when LCC data is sparse; do not overclaim confidence. The recommendation cites three evidence streams so any single sparse stream cannot drive the conclusion alone. |
</threat_model>

<verification>
After both tasks: two markdown fragments exist under `.planning/research/v2/_drafts/`. Recommendation cleanly chooses one of A/B/C with three evidence streams cited.
</verification>

<success_criteria>
- D-09: Three proposals evaluated; single recommendation committed
- D-21: Recommendation lives in a fragment that will become `CORPUS_SOURCING.md §"Genre set recommendation"`
- Three evidence streams documented (LCC overlap, comparable projects, author availability)
- Final genre count (8 or 10) committed — sets D-06's number (30 or 25)
- Snake-case labels if Proposal A wins
</success_criteria>

<output>
After completion, create `.planning/phases/07-corpus-sourcing-research-spike/07-02-SUMMARY.md` listing the recommendation, the chosen genre count (8 or 10), and the implied D-06 number (30 or 25) so Plans 03 and 05 know which constants to use.
</output>