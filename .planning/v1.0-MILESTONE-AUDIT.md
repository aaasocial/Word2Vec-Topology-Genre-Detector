---
milestone: v1.0
audited: 2026-05-24T22:30:00Z
status: gaps_found
scope: retrospective audit of shipped milestone (deployed 2026-04-13, live ~6 weeks at production)
scores:
  requirements: 58/63
  phases: 5/5
  integration: 4/5
  flows: 4/5
gaps:
  requirements:
    - id: "PARAM-03"
      status: "partial"
      phase: "04"
      claimed_by_plans: ["04-03"]
      completed_by_plans: ["04-03"]
      verification_status: "missing"
      evidence: "SlowTierParams.handleRecompute (line 59-68) calls console.log('Recompute triggered with:', changed) only — useRecompute.triggerRecompute is never invoked. Backend POST /api/viz/recompute is fully implemented (viz.py line 315) with PARAM_DEPENDENCY_MAP and 429 concurrency guard, but receives zero calls. Marked Validated in traceability table but the user-facing parameter-change-recomputes-cache loop is severed."
    - id: "PARAM-04"
      status: "partial"
      phase: "04"
      claimed_by_plans: ["04-03"]
      completed_by_plans: ["04-03"]
      verification_status: "missing"
      evidence: "VerySlowTierParams.handleConfirmRetrain (line 40-45) calls setIsRetraining(true) but no useRecompute.triggerRetrain call follows. The confirm-dialog UX works, the overlay renders, but the actual retrain HTTP request is never sent."
    - id: "PARAM-05"
      status: "partial"
      phase: "04"
      claimed_by_plans: ["04-03"]
      completed_by_plans: ["04-03"]
      verification_status: "missing"
      evidence: "RecomputeOverlay component renders correctly when isRecomputing or isRetraining is true in the Zustand store, but those flags are never set to true by any real recompute request because the upstream wiring (PARAM-03/04) is broken."
    - id: "PARAM-06"
      status: "partial"
      phase: "04"
      claimed_by_plans: ["04-03"]
      completed_by_plans: ["04-03"]
      verification_status: "missing"
      evidence: "Backend PARAM_DEPENDENCY_MAP in viz.py is fully implemented (correctly maps parameter changes to invalidation scope). Frontend never calls the endpoint, so the dependency map's runtime behaviour is unverified outside unit tests."
    - id: "EXPLAIN-01"
      status: "partial"
      phase: "04"
      claimed_by_plans: ["04-03"]
      completed_by_plans: ["04-03"]
      verification_status: "passed (Phase 4 plan-level)"
      evidence: "PipelineExplanation dialog renders 6 illustrative/static steps with keyboard navigation. The requirement text ('shows the user's actual uploaded/selected book's data') is interpreted in the implementation as a generic walkthrough using hardcoded examples, not the live book's pipeline trace. Scope-interpretation gap, not a wiring break."
  integration:
    - finding: "useRecompute.ts hook is dead code in v1.0"
      detail: "Hook exports triggerRecompute, triggerRetrain, cancelRecompute (lines 11, 107) — referenced only in source-code comments of SlowTierParams.tsx and VerySlowTierParams.tsx; never imported by any component. Zero import sites in frontend/src/."
      affects: ["PARAM-03", "PARAM-04", "PARAM-05", "PARAM-06"]
  flows:
    - flow: "Recompute slow-tier parameters → server recomputes → view updates"
      status: "broken in git"
      breaks_at: "SlowTierParams.handleRecompute (console.log only) / VerySlowTierParams.handleConfirmRetrain (sets flag but no HTTP call)"
      steps_complete: ["settings drawer opens", "parameter sliders change values", "dirty badge appears", "Recompute button enables"]
      steps_missing: ["useRecompute hook called", "POST /api/viz/recompute sent", "SSE progress subscribed", "React Query invalidated", "visualization updated"]
      production_impact: "unknown — broken flow has been live ~6 weeks; either users don't exercise this surface, or they do and have learned to work around it"
tech_debt:
  - phase: "all v1.0"
    items:
      - "Phases 2, 4, 5 do NOT have phase-level VERIFICATION.md (process artifact gap — GSD's auto-VERIFICATION step was added between when these phases shipped and when phase 6 shipped). Plan-level SUMMARY.md files exist for every plan."
      - "Phase 1 has a VERIFICATION.md predating the modern frontmatter format; readable but doesn't slot cleanly into the 3-source cross-reference matrix."
  - phase: "03 (frontend core)"
    items:
      - "TypeScript build emits 2 type errors (ScatterCanvas.tsx:30 TS2739 raycaster partial type; vitest.config.ts:6 TS2769 Vite version conflict). Build still produces dist/ successfully. Documented in 03-VERIFICATION.md as accepted at ship time."
  - phase: "02/04 (cache layer)"
    items:
      - "cache_key did not include corpus_hash or w2v_model_sha256 — latent staleness footgun. Documented in ROADMAP.md as BUG-05; addressed in Phase 6 (v2.0)."
  - phase: "03 (BookSlider)"
    items:
      - "BookSlider shipped with books={[]} stub in Phase 3. Documented as v1 carry-over; addressed in Phase 6 BUG-03 (v2.0)."
  - phase: "04 (homology UI)"
    items:
      - "H₂ tab disabled with non-firing tooltip; H₁ persistence-diagram dot-scaling unreadable. Documented as v1 carry-over; addressed in Phase 6 BUG-01/BUG-02 (v2.0)."
nyquist:
  compliant_phases: 0
  partial_phases: 5
  missing_phases: 0
  overall: "partial"
  notes: "All 5 v1.0 phases have VALIDATION.md files but they predate the current Nyquist compliance schema — frontmatter does not include nyquist_compliant or wave_0_complete fields. Treating as partial coverage retroactively. Re-running /gsd-validate-phase on each v1.0 phase is not recommended (shipped code, would be retrospective theatre)."
---

# v1.0 Milestone Audit (Retrospective)

**Audited:** 2026-05-24 (~6 weeks after deployment)
**Status:** `gaps_found`
**Production URL:** https://word2vec-topology-genre-detector-production.up.railway.app

## Summary

v1.0 was deployed to Railway on 2026-04-13 and has been live for ~6 weeks. The retrospective audit confirms the **primary user flow (upload → classify → visualize) is fully wired and functional**, but surfaces five requirements that were marked "Validated" in the traceability table but which integration analysis shows are partially satisfied or have orphaned wiring:

- **PARAM-03..06** (slow-tier parameter recompute loop): backend endpoint is correct and complete; frontend `useRecompute.ts` hook exists but no component imports it. The "Recompute" button logs to console instead of calling the API. Broken in git; production behaviour depends on whether users exercise this surface.
- **EXPLAIN-01** (pipeline explanation): renders 6 illustrative steps with hardcoded data rather than the user's live uploaded book data per the requirement text. Scope-interpretation gap.

## Requirement Coverage

| Phase | Reqs Total | Satisfied | Partial | Unsatisfied | Notes |
|-------|------------|-----------|---------|-------------|-------|
| 1 — Pipeline Validation Spike | 17 | 17 | 0 | 0 | All wired or self-contained. VERIFICATION.md present. |
| 2 — API Layer + Job Queue | 10 | 10 | 0 | 0 | All wired. VERIFICATION.md missing (tech debt). |
| 3 — Frontend Core + 3D Viz | 16 | 16 | 0 | 0 | All wired. VERIFICATION.md present. |
| 4 — Advanced Viz + Params | 17 | 12 | 5 | 0 | PARAM-03/04/05/06 + EXPLAIN-01 partial. VERIFICATION.md missing. |
| 5 — Deployment | 2 | 2 | 0 | 0 | Dockerfile/Railway/SSE all wired. VERIFICATION.md missing. |
| **Total** | **62** | **57** | **5** | **0** | One requirement off the 63 total — VIZ-05 (BookSlider) shipped as acknowledged stub, now fixed in v2.0 Phase 6. |

## Cross-Phase Wiring

| Phase Pair | Wiring | Status |
|------------|--------|--------|
| 1 → 2 | `scripts/0{1..6}_*.py` → `backend/pipeline/*.py` | OK |
| 2 → 3 | 8/9 API routes consumed; 1 orphaned (`POST /api/viz/recompute`) | OK with gap |
| 3 → 4 | All Phase 4 components properly routed and integrated | OK |
| 5 → all | Dockerfile + Railway + GitHub Release asset + SSE | OK |

## E2E Flows

| Flow | Status |
|------|--------|
| Upload .txt → classify → top genre + confidence | ✓ Wired |
| Browse genre → scatter populates → hover word details | ✓ Wired |
| Open topology tab → see persistence heatmap | ✓ Wired |
| Adjust ε-slider → VR animation updates | ✓ Wired |
| Recompute slow-tier parameters → server recomputes → view updates | ✗ Broken in git (PARAM-03..06) |

## Audit Recommendation

The primary user-facing functionality of v1.0 works end-to-end in production. The recompute path gap is real but affects a power-user feature that the production deployment has tolerated for 6 weeks without incident — suggesting either low usage or users adapting around it.

**Recommended next move:** decide between
1. **Fix the gap in a v1.0.1 patch phase** before archiving — wires `useRecompute` into `SlowTierParams` + `VerySlowTierParams`, replaces console.log with the actual hook call. ~30 min of work; affects 2 component files. Cleanest archive.
2. **Accept as tech debt and archive v1.0 as-shipped** — record PARAM-03..06 status as "partial — frontend wiring gap, defer to v2.0 Phase 10 (Visual Polish, which already touches these components)". Honest history; surfaces the gap in the archive for future maintainers.
3. **Defer to v2.0 Phase 10 explicitly** — Phase 10's "dark-mode sweep across ~30 components touched by Phases 6-9" includes SlowTierParams and VerySlowTierParams. Add the useRecompute wiring as a sub-task there; archive v1.0 with a forward-pointer to the fix.

The integration checker recommended `gaps_found`. The honest answer is **gaps exist but they were latent and tolerated in production**; the choice between patch-now vs accept-as-tech-debt is a judgment call about how clean you want the v1.0 archive to be.
