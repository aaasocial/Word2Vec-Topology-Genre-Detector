gensim==4.4.0
# Models (TfidfVectorizer, SVM, KMeans, scalers) were pickled with sklearn
# 1.6.1. Loading them under 1.3.x silently strips fitted attributes -- most
# visibly `idf_` on TfidfTransformer -- which surfaces as "idf vector is not
# fitted" the moment the classify worker tries to vectorize an uploaded book.
# Pin to 1.6.1 to keep the runtime sklearn matched to what trained the models.
scikit-learn==1.6.1
scipy>=1.11.0
numpy>=1.24.0
# giotto-tda was the original homology library but Phase 2 switched the
# runtime path to ripser/persim (see .planning/PROJECT.md Key Decisions and
# scripts/02-03-SUMMARY.md). Nothing in backend/ imports gtda, and the
# Dockerfile does not copy scripts/04*/05* or tests/, so giotto-tda is dead
# weight in the deployed image -- and its scikit-learn==1.3.2 hard pin is
# precisely what forced the version skew above. Kept available locally for
# the offline precompute scripts via pip install giotto-tda==0.6.2.
persim==0.3.8
nltk>=3.8.0
pyyaml>=6.0
gutenbergpy>=0.3.5
joblib>=1.3.0
pytest>=8.0.0
fastapi==0.135.3
uvicorn==0.44.0
arq==0.27.0
redis>=4.2.0,<6
python-multipart==0.0.26
chardet==7.4.1
langdetect==1.0.9
httpx
pytest-asyncio
umap-learn>=0.5.0
